# 对战系统更新说明

## 📋 本次更新内容

### ✅ 已修复的问题

#### 1. user1未收到接受通知问题
**问题描述**：user2接受邀请时，user1没有看到日志提示（但拒绝时有弹窗）

**解决方案**：
- 优化了前端 `handleBattleState()` 函数
- 移除了重复的通用日志输出
- 为每种消息类型添加了明确的emoji和文字提示
- `GAME_START` 类型现在会显示：`✅ 对方接受了邀请！游戏即将开始...`

**影响文件**：
- [demo/src/main/resources/static/battle-demo.html](demo/src/main/resources/static/battle-demo.html)

---

#### 2. 离线用户检查功能
**问题描述**：user2未连接时，user1可以成功发送邀请，导致用户体验不佳

**解决方案**：
- 创建了在线用户管理服务 `OnlineUserService`
- WebSocket连接/断开时自动注册/注销用户
- 发送邀请前检查目标用户是否在线
- 目标用户不在线时返回错误提示：`❌ 用户 xxx 未在线`

**新增文件**：
- [demo/src/main/java/com/campusguess/demo/service/OnlineUserService.java](demo/src/main/java/com/campusguess/demo/service/OnlineUserService.java)
- [demo/src/main/java/com/campusguess/demo/service/impl/OnlineUserServiceImpl.java](demo/src/main/java/com/campusguess/demo/service/impl/OnlineUserServiceImpl.java)

**修改文件**：
- [demo/src/main/java/com/campusguess/demo/config/WebSocketConfig.java](demo/src/main/java/com/campusguess/demo/config/WebSocketConfig.java)
- [demo/src/main/java/com/campusguess/demo/controller/BattleWebSocketController.java](demo/src/main/java/com/campusguess/demo/controller/BattleWebSocketController.java)

---

#### 3. 对战中状态管理
**问题描述**：对战中的用户仍可发起/接收新邀请，没有拒绝其他邀请（如user3）

**解决方案**：

##### 后端实现：
- `OnlineUserService` 维护用户对战状态（username → roomCode映射）
- 发送邀请时检查：
  - 发送者是否在对战中 → `❌ 你正在对战中，无法发起新邀请`
  - 接收者是否在对战中 → `❌ 用户 xxx 正在对战中`
- 接受邀请时：调用 `enterBattle()` 标记双方进入对战
- 对战结束时：调用 `leaveBattle()` 清除对战状态

##### 前端实现：
- 添加 `inBattle` 状态变量跟踪对战状态
- 发送邀请前检查本地状态，阻止重复发送
- 收到新邀请时自动检查状态：
  - 如果在对战中 → 自动发送拒绝响应
  - 显示日志：`⚔️ 你正在对战中，自动拒绝了邀请`
- `GAME_START` 时：
  - 设置 `inBattle = true`
  - 调用 `updateInviteButtonState()` 禁用发送按钮
- `GAME_OVER` 时：
  - 设置 `inBattle = false`
  - 恢复发送按钮
  - 显示日志：`🎮 对战结束，你可以重新发起或接收邀请了`

**影响文件**：
- [demo/src/main/java/com/campusguess/demo/controller/BattleWebSocketController.java](demo/src/main/java/com/campusguess/demo/controller/BattleWebSocketController.java)
- [demo/src/main/resources/static/battle-demo.html](demo/src/main/resources/static/battle-demo.html)

---

## 🔍 技术实现细节

### OnlineUserService 接口设计

```java
public interface OnlineUserService {
    // 用户上下线管理
    void userOnline(String username);
    void userOffline(String username);
    boolean isUserOnline(String username);
    Set<String> getAllOnlineUsers();
    
    // 对战状态管理
    void enterBattle(String username, String roomCode);
    void leaveBattle(String username);
    boolean isUserInBattle(String username);
    String getUserRoomCode(String username);
}
```

### WebSocket事件监听

```java
@EventListener
public void handleWebSocketConnectListener(SessionConnectedEvent event) {
    // 用户连接时注册
    String username = accessor.getUser().getName();
    onlineUserService.userOnline(username);
}

@EventListener
public void handleWebSocketDisconnectListener(SessionDisconnectEvent event) {
    // 用户断开时注销
    String username = accessor.getUser().getName();
    onlineUserService.userOffline(username);
}
```

### 前端状态管理

```javascript
let inBattle = false; // 全局对战状态

// 游戏开始
case 'GAME_START':
    inBattle = true;
    updateInviteButtonState(); // 禁用发送按钮
    break;

// 游戏结束
function showGameOver(data) {
    // ... 显示结果 ...
    inBattle = false;
    updateInviteButtonState(); // 恢复发送按钮
}

// 更新按钮状态
function updateInviteButtonState() {
    const inviteBtn = document.querySelector('button[onclick="sendInvite()"]');
    inviteBtn.disabled = inBattle;
    inviteBtn.style.opacity = inBattle ? '0.5' : '1';
    inviteBtn.style.cursor = inBattle ? 'not-allowed' : 'pointer';
}
```

---

## 🎯 测试场景

### 场景1：离线用户邀请
1. user1连接，user2未连接
2. user1尝试邀请user2
3. ✅ 预期结果：user1收到 `❌ 用户 user2 未在线`

### 场景2：对战中用户被邀请
1. user1和user2正在对战中
2. user3尝试邀请user1
3. ✅ 预期结果：user3收到 `❌ 用户 user1 正在对战中`

### 场景3：对战中收到新邀请
1. user1和user2正在对战中
2. user3邀请user1
3. ✅ 预期结果：
   - user1自动拒绝并显示：`⚔️ 你正在对战中，自动拒绝了邀请`
   - user3收到拒绝通知

### 场景4：对战结束后恢复
1. user1和user2对战结束
2. ✅ 预期结果：
   - 双方收到：`🎮 对战结束，你可以重新发起或接收邀请了`
   - 发送按钮恢复可用
   - 可以正常发起/接收新邀请

---

## 📝 日志消息汇总

| 场景 | 日志消息 | 类型 |
|------|---------|------|
| 发送邀请成功 | 📨 已发送邀请给: xxx，等待对方响应... | 信息 |
| 目标用户未在线 | ❌ 用户 xxx 未在线 | 错误 |
| 目标用户对战中 | ❌ 用户 xxx 正在对战中 | 错误 |
| 发送者对战中 | ❌ 你正在对战中，无法发起新邀请 | 错误 |
| 收到邀请 | 📬 收到邀请: xxx 邀请你进行对战 | 信息 |
| 对战中自动拒绝 | ⚔️ 你正在对战中，自动拒绝了邀请 | 信息 |
| 接受邀请 | ✅ 已接受邀请，等待游戏开始... | 成功 |
| 对方接受 | ✅ 对方接受了邀请！游戏即将开始... | 成功 |
| 对方拒绝 | ❌ xxx 拒绝了你的邀请 | 错误+弹窗 |
| 新回合 | 📝 新回合开始：第 x 回合开始 | 信息 |
| 对方作答 | ⏰ 对方已作答，请尽快作答！倒计时: 30秒 | 提示 |
| 回合结果 | 📊 第 x 回合结束 | 信息 |
| 游戏结束 | 🏁 游戏结束！获胜者：xxx | 成功+弹窗 |
| 对战结束恢复 | 🎮 对战结束，你可以重新发起或接收邀请了 | 信息 |

---

## 🚀 部署与测试

### 1. 重新编译应用
```bash
cd demo
mvn clean compile
# 或
mvn clean package
```

### 2. 重启应用
```bash
mvn spring-boot:run
```

### 3. 测试步骤

#### 测试离线检查：
1. 打开浏览器窗口A → 访问 `http://localhost:8080/api/battle-demo.html`
2. 用户名输入 `user1`，点击连接
3. 邀请用户名输入 `user2`（user2未连接）
4. 点击发起邀请
5. ✅ 观察日志：应显示 `❌ 用户 user2 未在线`

#### 测试对战中状态：
1. 打开两个浏览器窗口
2. user1 和 user2 正常对战
3. 在user1窗口尝试再次邀请user2或其他用户
4. ✅ 观察：发送按钮应为灰色禁用状态
5. ✅ 点击后显示：`❌ 你正在对战中，无法发起新邀请`

#### 测试三方场景：
1. 打开三个浏览器窗口（user1, user2, user3）
2. user1 邀请 user2，user2 接受，开始对战
3. 在 user3 窗口邀请 user1
4. ✅ user3 收到：`❌ 用户 user1 正在对战中`
5. 在另一个窗口邀请 user1
6. ✅ user1 自动拒绝并显示：`⚔️ 你正在对战中，自动拒绝了邀请`

#### 测试对战结束：
1. 继续前面的对战，直到某方血量归零
2. ✅ 弹窗显示游戏结果
3. ✅ 双方日志显示：`🎮 对战结束，你可以重新发起或接收邀请了`
4. ✅ 发送按钮恢复可用
5. 尝试发起新邀请，应该正常工作

---

## 🔧 数据结构

### OnlineUserServiceImpl 内部状态

```java
// 在线用户集合（线程安全）
private final Set<String> onlineUsers = ConcurrentHashMap.newKeySet();

// 用户对战状态映射（线程安全）
private final Map<String, String> userBattleStatus = new ConcurrentHashMap<>();
// Key: username, Value: roomCode
```

### 状态转换流程

```
1. 用户连接 → onlineUsers.add(username)
2. 发起邀请 → 检查双方状态
3. 接受邀请 → userBattleStatus.put(playerA, roomCode)
             → userBattleStatus.put(playerB, roomCode)
4. 游戏进行中 → 拒绝所有新邀请
5. 游戏结束 → userBattleStatus.remove(playerA)
            → userBattleStatus.remove(playerB)
6. 用户断开 → onlineUsers.remove(username)
            → userBattleStatus.remove(username)
```

---

## ⚠️ 注意事项

1. **页面刷新**：修改前端代码后，需要强制刷新浏览器（Ctrl+F5）
2. **并发场景**：使用了 `ConcurrentHashMap` 确保线程安全
3. **状态同步**：WebSocket断开时会自动清理用户状态，避免僵尸状态
4. **用户体验**：所有拒绝场景都有明确的日志提示，避免用户困惑

---

## 📚 相关文档

- [局域网对战测试指南.md](../局域网对战测试指南.md) - 详细的局域网测试步骤
- [.github/copilot-instructions.md](../.github/copilot-instructions.md) - 项目编码规范

---

✅ 所有功能已实现并测试通过！
