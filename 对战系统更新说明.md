# 对战记录功能更新说明

## 📝 更新概述

**更新日期**: 2026年1月12日  
**功能**: 为双人对战功能增加游戏记录保存功能

---

## 🎯 功能说明

对战结束后,系统会自动为双方玩家各创建一条游戏记录(Record),并保存每回合的详细答题信息到现有的 `records` 和 `record_items` 表中。

### 数据存储结构

```
BattleRoom (对战房间)
    ├── roundHistoryJson (TEXT字段,存储回合记录的JSON)
    │   └── [BattleRoundRecord, ...] (临时对象列表,不持久化)
    │
    └── 对战结束后 → 解析JSON → 生成Record
         ├── Record (玩家A的游戏记录)
         │   └── RecordItem[] (每回合的答题记录)
         │
         └── Record (玩家B的游戏记录)
             └── RecordItem[] (每回合的答题记录)
```

---

## 🆕 新增内容

### 1. **BattleRoundRecord.java** (临时对象,不持久化)
**路径**: `demo/src/main/java/com/campusguess/demo/model/entity/BattleRoundRecord.java`

**作用**: 作为临时数据结构,在内存中暂存每回合信息

**特点**:
- ⚠️ **不是数据库实体** (移除了 `@Entity` 注解)
- 游戏过程中序列化为JSON存储在 `BattleRoom.roundHistoryJson` 中
- 游戏结束时反序列化,用于生成 `Record` 和 `RecordItem`

**字段说明**:
```java
Integer roundNumber          // 回合数
Long questionId              // 题目ID
Double playerALon/Lat        // 玩家A的答案坐标
Double playerBLon/Lat        // 玩家B的答案坐标
Double playerADistance       // 玩家A的距离(米)
Double playerBDistance       // 玩家B的距离(米)
String damagedPlayer         // 被扣血的玩家
Integer damage               // 扣除的血量
Integer playerAHealthAfter   // 回合后玩家A的血量
Integer playerBHealthAfter   // 回合后玩家B的血量
```

---

## 🔄 修改的文件

### 1. **BattleRoom.java**
**修改内容**:
- ✅ 添加 `String roundHistoryJson` 字段 (TEXT类型,存储回合历史JSON)
- ❌ 移除 `List<BattleRoundRecord> roundRecords` 关联关系

**数据库字段**:
```sql
ALTER TABLE battle_rooms 
ADD COLUMN round_history_json TEXT;
```

### 2. **BattleServiceImpl.java**
**修改内容**:
- 移除 `BattleRoundRecordRepository` 依赖
- 添加 `ObjectMapper` 用于JSON序列化

**新增方法**:
```java
// 将回合记录保存到JSON
private void saveRoundRecordToJson(BattleRoom room, BattleRoundRecord roundRecord)

// 从JSON读取回合记录
private List<BattleRoundRecord> getRoundRecordsFromJson(BattleRoom room)
```

**修改流程**:
1. 每回合结束 → 创建 `BattleRoundRecord` 临时对象 → 序列化追加到 `roundHistoryJson`
2. 游戏结束 → 反序列化 `roundHistoryJson` → 保存到 `records` 和 `record_items` 表

### 3. **删除文件**
- ❌ `BattleRoundRecordRepository.java` (不再需要)

---

## 📊 数据表变化

### 修改表: `battle_rooms`
**新增字段**:
```sql
round_history_json TEXT  -- 存储回合记录的JSON数组
```

**JSON格式示例**:
```json
[
  {
    "roundNumber": 1,
    "questionId": 123,
    "playerALon": 113.123,
    "playerALat": 23.456,
    "playerBLon": 113.234,
    "playerBLat": 23.567,
    "playerADistance": 1234.5,
    "playerBDistance": 567.8,
    "damagedPlayer": "alice",
    "damage": 20,
    "playerAHealthAfter": 80,
    "playerBHealthAfter": 100
  },
  {
    "roundNumber": 2,
    ...
  }
]
```

### 使用现有表: `records` 和 `record_items`
**无需新增表**,对战记录直接保存到现有的游戏记录表中。

**对战记录特点**:
- `gameType` = "BATTLE"
- `earnPoints` = 0 (对战不计算积分)
- `pointBefore` = `pointAfter` (积分不变)

---

## 🔍 业务逻辑

### 回合记录流程

1. **回合进行中**: 
   - 双方提交答案 → 计算距离和伤害
   - 创建 `BattleRoundRecord` 临时对象
   - 序列化为JSON追加到 `BattleRoom.roundHistoryJson`

2. **游戏结束**:
   - 从 `roundHistoryJson` 反序列化获取所有回合记录
   - 为双方玩家各创建一条 `Record`
   - 每个回合生成一条 `RecordItem`
   - 保存到数据库

### 记录保存时机

1. **正常结束**: 某方血量归零 → 自动保存双方记录
2. **玩家退出**: 玩家主动退出 → 自动保存双方记录

---

## ✅ 优势

### 相比持久化 `battle_round_records` 表:

1. **简化数据库结构**: 不需要额外的表和外键关系
2. **减少数据库查询**: 回合数据存储在一个字段中,减少JOIN查询
3. **统一记录管理**: 所有游戏记录(单人/对战)都在 `records` 表中
4. **灵活性**: JSON格式易于扩展,添加新字段不需要数据库迁移

---

## 🎮 前端对接说明

### 查询对战记录

**使用现有接口**:
```http
GET /api/users/{username}/records
```

**响应示例**:
```json
[
  {
    "id": 100,
    "userId": 1,
    "totalQuestionNum": 3,
    "earnPoints": 0,
    "gameType": "BATTLE",
    "createdAt": "2026-01-12T14:30:00"
  }
]
```

**区分对战记录**:
- `gameType` = "BATTLE" → 对战记录
- `earnPoints` = 0 → 不计算积分
- `totalQuestionNum` = 对战回合数

### 查询对战详情

**使用现有接口**:
```http
GET /api/records/{recordId}
```

**响应包含**:
- 每回合的题目信息
- 玩家的答案坐标
- 可以根据坐标计算与正确答案的距离

---

## ⚠️ 注意事项

### 1. 数据库迁移
启动应用后,Hibernate会自动在 `battle_rooms` 表中添加 `round_history_json` 字段。

### 2. JSON序列化
- 使用 `ObjectMapper` 进行JSON序列化/反序列化
- 如果JSON解析失败,会记录日志但不影响游戏流程

### 3. 数据大小限制
- `TEXT` 字段最大存储 ~64KB
- 假设每回合JSON约200字节,可存储约300回合(实际对战一般不超过10回合)

---

## 🧪 测试建议

### 测试场景
1. ✅ 正常对战结束(血量归零)
2. ✅ 玩家主动退出
3. ✅ 多回合对战记录完整性
4. ✅ 双方记录独立性
5. ✅ JSON序列化/反序列化正确性

### 验证步骤
1. 完成一场对战(3-5回合)
2. 查询 `battle_rooms` 表,检查 `round_history_json` 字段
3. 查询双方的 `records` 表,确认 `gameType` = "BATTLE"
4. 查询 `record_items`,确认数量 = 对战回合数
5. 确认双方的答案坐标不同

---

## 📞 技术支持

如有问题,请检查日志中的关键信息:
```
对战记录已保存: 房间={roomCode}, 玩家A={playerA}, 玩家B={playerB}
```

如果记录保存失败,日志会显示:
```
保存对战记录失败: {错误信息}
从JSON读取回合记录失败: {错误信息}
```
