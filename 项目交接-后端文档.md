# CampusGuessSystem 后端交接文档

本文档是后端接手的唯一参考文档，聚焦服务端架构、接口契约、数据持久化、实时对战状态和集群化部署。前端任务请单独查看前端交接文档，二者分支和职责必须隔离。

## 1. 项目定位

项目是"校园图寻"多人实时对战游戏的延续与优化版本。当前阶段的核心目标不是继续堆功能，而是把现有单体后端改造成可扩展、可恢复、可集群部署的系统。

最重要的两个后端目标：

1. ~~将游戏状态从单机内存迁移到 Redis，解决房间、在线状态、作答状态和临时回合数据在单实例上的局限。~~ **✅ 已完成（2026-07-05）**
2. ~~验证 WebSocket 集群化部署原型，至少完成 Nginx 反向代理 + 多 Spring Boot 实例的协同工作验证。~~ **✅ 已完成（2026-07-05）**

## 2. 代码结构与技术栈

当前后端主要在 [demo/src/main/java/com/campusguess/demo](demo/src/main/java/com/campusguess/demo) 下，按分层架构组织：

- controller：HTTP 和 WebSocket 控制器
- service / service/impl：业务接口和实现
- repository：JPA 仓库
- model/entity：实体类
- model/dto：请求、响应和消息 DTO
- config：安全、WebSocket、序列化、Redis 等配置
- exception：业务异常与全局异常处理

运行环境与基础配置：

- Spring Boot 4.0.1
- Java 17
- MySQL + Spring Data JPA
- WebSocket 使用 STOMP 协议
- 默认端口 8080，统一上下文路径 /api
- Redis（在线状态、房间缓存、分布式锁）
- RabbitMQ（外部 STOMP Broker，集群模式）
- Nginx（反向代理 + 负载均衡，集群模式）

关键配置文件是 [demo/src/main/resources/application.yml](demo/src/main/resources/application.yml)，其中包含数据库连接、JPA 配置、JWT 密钥、Redis 连接、Broker 模式和图片存储服务信息。接手后如果要做正式部署，优先把敏感配置迁移到环境变量或外部配置源。

## 3. 运行环境搭建（Docker 组件）

本项目依赖以下外部组件，强烈建议使用 Docker 统一管理。

### 3.1 前置条件

| 软件 | 版本要求 | 用途 |
|------|---------|------|
| JDK | 17+ | 编译与运行 Spring Boot |
| Maven | 3.8+ | 项目构建 |
| Docker | 20.10+ | 运行 RabbitMQ |
| MySQL | 8.0+ | 持久化数据（可本地安装或 Docker） |
| Redis | 7.0+ | 在线状态 / 房间缓存 / 分布式锁 |
| Nginx | 1.24+ | 反向代理与负载均衡 |

### 3.2 启动 MySQL

如果本地已有 MySQL 8.0，确保数据库 `campusguesssystem` 已创建：

```sql
CREATE DATABASE IF NOT EXISTS campusguesssystem CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
```

如果使用 Docker 运行 MySQL：

```bash
docker run -d --name mysql-campus `
  -p 3306:3306 `
  -e MYSQL_ROOT_PASSWORD=134679*Abc `
  -e MYSQL_DATABASE=campusguesssystem `
  mysql:8.0
```

### 3.3 启动 Redis

**Windows 本地安装**（推荐开发环境）：

```bash
# 下载 Redis for Windows 或使用 WSL
redis-server
# 默认端口 6379，无需密码（与 application.yml 一致）
```

**Docker 方式**：

```bash
docker run -d --name redis-campus `
  -p 6379:6379 `
  redis:7-alpine
```

验证 Redis 连通性：

```bash
redis-cli PING
# 预期输出: PONG
```

### 3.4 启动 RabbitMQ（集群模式必需）

RabbitMQ 作为外部 STOMP Broker，是集群化部署的关键组件。**单机模式（`broker.type=simple`）不需要 RabbitMQ。**

```bash
# 拉取带管理插件的镜像
docker run -d --name rabbitmq-campus `
  -p 5672:5672 `
  -p 15672:15672 `
  -p 61613:61613 `
  rabbitmq:3-management

# 等待 RabbitMQ 完全启动后，启用 STOMP 插件
docker exec rabbitmq-campus rabbitmq-plugins enable rabbitmq_stomp

# 验证管理界面
# 浏览器打开 http://localhost:15672
# 用户名/密码: guest/guest
```

端口说明：

| 端口 | 用途 |
|------|------|
| 5672 | AMQP 协议 |
| 15672 | 管理界面 |
| 61613 | STOMP 协议（Spring STOMP Broker 连接用） |

### 3.5 安装 Nginx（集群模式必需）

**Windows**：下载 [Nginx for Windows](https://nginx.org/en/download.html)，解压到 `D:\nginx\nginx-1.30.3\`（或其他路径，需同步修改 `start-cluster.bat`）。

**配置文件**：项目根目录 [demo/nginx-cluster.conf](demo/nginx-cluster.conf) 已提供完整的集群化 Nginx 配置，使用前需复制到 Nginx 安装目录：

```bash
copy /Y demo\nginx-cluster.conf D:\nginx\nginx-1.30.3\conf\nginx-cluster.conf
```

Nginx 配置要点：

- HTTP API 使用 `ip_hash` 策略，保证同一客户端请求路由到同一后端
- WebSocket 使用 `least_conn` 策略，将新连接分配到连接数最少的实例
- WebSocket 代理禁用了缓冲（`proxy_buffering off`），确保实时传输
- 长连接超时设为 3600 秒

## 4. 已实现的后端核心模块

### 4.1 用户与认证

用户相关接口集中在 [UserController](demo/src/main/java/com/campusguess/demo/controller/UserController.java) 和对应 service 中，当前支持注册、查询用户信息和修改积分。

认证链路保留了 JWT 配置，但当前整体偏开发态，权限控制没有按生产环境标准完全收紧。

### 4.2 题目与图片

题目接口集中在 [QuestionController](demo/src/main/java/com/campusguess/demo/controller/QuestionController.java) 和对应 service 中，支持创建题目、获取题目详情、分页查询、按用户查询和删除题目。

题目实体使用 imageKey 保存图片标识，不直接保存完整 URL，前端按图床域名拼接展示。

### 4.3 游戏记录

记录接口集中在 [RecordController](demo/src/main/java/com/campusguess/demo/controller/RecordController.java) 和 [RecordServiceImpl](demo/src/main/java/com/campusguess/demo/service/impl/RecordServiceImpl.java)，支持提交记录、查询用户记录和查看详情。

对战结束后，回合数据会进入 Record / RecordItem，适合后续做排行、回放和统计。

### 4.4 好友与评论

仓库中还存在好友、评论和点赞模块，说明项目不只是对战玩法，而是朝完整校园社交场景演进。当前后端接手优先级仍应放在对战主链路和状态一致性上。

## 5. 实时对战后端链路

### 5.1 WebSocket 服务端入口

WebSocket 配置在 [WebSocketConfig](demo/src/main/java/com/campusguess/demo/config/WebSocketConfig.java)。当前设计特点：

- STOMP 端点为 /api/ws-battle
- 客户端必须通过 URL 参数 username 传入身份
- 支持 SockJS 降级
- 客户端发送前缀为 /app
- 个人消息队列为 /user/queue/battle/state
- 支持两种 Broker 模式：
  - `simple`：单机 SimpleBroker（开发/单实例）
  - `external`：外部 STOMP Broker（集群/多实例，需 RabbitMQ）

握手阶段会把 username 注入 Principal，这样 convertAndSendToUser 才能正确路由。

### 5.2 在线状态管理

在线状态由 [OnlineUserServiceImpl](demo/src/main/java/com/campusguess/demo/service/impl/OnlineUserServiceImpl.java) 维护，**已从纯内存实现迁移到 Redis**。内部数据结构：

- 在线用户集合 → Redis Set（`campus:online:users`）
- 用户到房间的映射 → Redis String（`campus:user:{username}:room`）
- 用户到 sessionId 的映射 → Redis String（`campus:user:{username}:session`）
- sessionId 到用户的映射 → Redis String（`campus:session:{sessionId}:user`）

所有在线状态数据均设置了 TTL（30 分钟），避免僵尸数据堆积。多实例共享同一 Redis 实例，状态天然同步。

### 5.3 战斗房间与回合流转

战斗房间实体在 [BattleRoom](demo/src/main/java/com/campusguess/demo/model/entity/BattleRoom.java) 中定义，保存 roomCode、玩家、血量、当前题目、回合数、作答状态、答案、房间状态、胜利者、时间戳、历史 JSON 和游戏模式。

当前状态流转：

1. 邀请方创建房间，状态为 WAITING。
2. 被邀请方接受后，状态变为 PLAYING，推送 GAME_START。
3. 双方提交答案后，先推送 PLAYER_ANSWERED，再计算回合结果。
4. 某方血量归零则结束对战并推送 GAME_OVER。
5. 否则 3 秒后进入下一回合并推送 NEW_QUESTION。

### 5.4 控制器职责

实时对战入口在 [BattleWebSocketController](demo/src/main/java/com/campusguess/demo/controller/BattleWebSocketController.java)，它负责：

- 发送邀请
- 响应邀请
- 提交答案（含分布式锁 + 答案 Redis 缓存）
- 主动退出
- 处理回合结果与下一回合（含锁释放 + 答案清除）

这一层既是消息路由层，也是部分流程编排层。**Redis 改造后，提交答案处增加了分布式锁（`tryLockRoom`/`unlockRoom`）防止并发重复结算，答案通过 `cachePlayerAnswer` 写入 Redis（TTL 60s），新回合开始时通过 `clearPlayerAnswers` 清除。**

### 5.5 房间缓存层

新增 [BattleRoomCacheService](demo/src/main/java/com/campusguess/demo/service/BattleRoomCacheService.java) 及其实现 [BattleRoomCacheServiceImpl](demo/src/main/java/com/campusguess/demo/service/impl/BattleRoomCacheServiceImpl.java)，封装所有 Redis 房间操作：

- `cacheRoomState` / `getRoomState` — 房间全量状态缓存（Hash，TTL 30min）
- `updateRoomStatus` — 增量更新作答状态等字段
- `cachePlayerAnswer` — 玩家答案缓存（String JSON，TTL 60s）
- `clearPlayerAnswers` — 清除双方答案缓存
- `tryLockRoom` / `unlockRoom` — 分布式锁（SET NX EX 10s）
- `evictRoom` — 房间结束时清理所有相关 key（state + answer + lock）

## 6. 后端数据存储现状

当前采用"**Redis 实时态** + 数据库持久态"的混合方案。

### 6.1 房间态

房间和对局过程会实时保存到数据库，同时缓存到 Redis Hash（`campus:room:{roomCode}:state`）。在线映射、战斗中状态、作答状态均通过 Redis 管理，不再依赖单机内存。

### 6.2 回合历史

每回合答案、距离、扣血和玩家血量会被封装成 BattleRoundRecord，并序列化为 JSON 保存到 BattleRoom 的 roundHistoryJson。

### 6.3 最终记录

对局结束时，BattleServiceImpl 会把回合历史转换为 Record 和 RecordItem 落库。积分排行模式还会根据血量差进行积分变化，默认模式下则偏向记录结果。

### 6.4 当前限制

- ~~在线状态是单机内存态~~ ✅ 已迁移到 Redis
- ~~重启后内存状态会丢失~~ ✅ Redis 持久化，重启不丢失
- 房间并发冲突通过分布式锁保护，但锁粒度较粗（整个房间级别）
- WebSocket 连接仍绑定单实例，会话无法跨节点迁移
- 依赖 JVM 存活（WebSocket 会话无法跨节点迁移）

## 7. 后端接口与消息契约

### 7.1 HTTP 侧

后端 HTTP 控制器主要包括：

| 控制器 | 路径前缀 | 主要功能 |
|--------|---------|---------|
| UserController | `/api/users` | 注册 `POST`、查询 `GET /{username}`、修改积分 `PUT /{username}/changePoint` |
| QuestionController | `/api/questions` | 创建题目、分页查询、按用户查询、删除 |
| RecordController | `/api/records` | 提交记录、查询用户记录、查看详情 |

当前约定是统一返回 ResponseEntity<ApiResponse<T>>，成功消息使用中文。

### 7.2 WebSocket 侧

服务端消息类型需要和前端严格对齐，主要包括：

| 消息类型 | 方向 | 说明 |
|----------|------|------|
| INVITE | 服务端→客户端 | 收到对战邀请 |
| INVITE_ACCEPTED | 服务端→客户端 | 邀请被接受 |
| INVITE_REJECTED | 服务端→客户端 | 邀请被拒绝 |
| GAME_START | 服务端→客户端 | 游戏开始 |
| NEW_QUESTION | 服务端→客户端 | 新回合题目 |
| PLAYER_ANSWERED | 服务端→客户端 | 对方已作答 |
| ROUND_RESULT | 服务端→客户端 | 回合结果 |
| GAME_OVER | 服务端→客户端 | 游戏结束 |

服务端发送路径：

- /user/queue/battle/invite
- /user/queue/battle/state

客户端发送路径：

- /app/battle/invite
- /app/battle/respond
- /app/battle/answer
- /app/battle/quit

## 8. 部署模式

### 8.1 单机开发模式

适用于本地开发和调试，无需 RabbitMQ 和 Nginx。

```bash
# 1. 确保 MySQL 和 Redis 已启动
# 2. 构建项目
cd demo
mvnw.cmd clean package -DskipTests

# 3. 启动（默认 broker.type=simple，online-user.store=redis）
java -jar target\demo-0.0.1-SNAPSHOT.jar

# 访问: http://localhost:8080/api
```

### 8.2 集群模式

适用于多实例部署，需要 RabbitMQ + Nginx 协同。

**启动流程**：

```bash
# 1. 确保 MySQL、Redis 已启动
# 2. 启动 RabbitMQ（如果未启动）
docker run -d --name rabbitmq-campus -p 5672:5672 -p 15672:15672 -p 61613:61613 rabbitmq:3-management
docker exec rabbitmq-campus rabbitmq-plugins enable rabbitmq_stomp

# 3. 构建项目
cd demo
mvnw.cmd clean package -DskipTests

# 4. 启动实例 1（端口 8080，external broker）
start "CampusGuessing-8080" cmd /c "java -jar target\demo-0.0.1-SNAPSHOT.jar --server.port=8080 --broker.type=external --online-user.store=redis"

# 5. 等待实例 1 启动（约 10 秒），再启动实例 2（端口 8081）
start "CampusGuessing-8081" cmd /c "java -jar target\demo-0.0.1-SNAPSHOT.jar --server.port=8081 --broker.type=external --online-user.store=redis"

# 6. 复制 Nginx 配置并启动
copy /Y nginx-cluster.conf D:\nginx\nginx-1.30.3\conf\nginx-cluster.conf
D:\nginx\nginx-1.30.3\nginx.exe -c D:\nginx\nginx-1.30.3\conf\nginx-cluster.conf

# 访问: http://localhost/api (通过 Nginx 负载均衡)
```

**一键启动**：项目根目录的 [demo/start-cluster.bat](demo/start-cluster.bat) 封装了以上全部步骤，包含环境检查和自动构建。

**停止集群**：

```bash
# 停止 Java 实例
taskkill /fi "WINDOWTITLE eq CampusGuessing-8080" /f
taskkill /fi "WINDOWTITLE eq CampusGuessing-8081" /f

# 停止 Nginx
taskkill /IM nginx.exe /f

# 停止 RabbitMQ（可选）
docker stop rabbitmq-campus
```

### 8.3 集群架构图

```
Browser STOMP Client
        │
        ▼
   Nginx :80 (least_conn for WS, ip_hash for HTTP)
        │
   ┌────┴────┐
   ▼         ▼
:8080      :8081         ← Spring Boot 实例
   │         │
   └────┬────┘
        ▼
RabbitMQ STOMP :61613    ← 外部 STOMP Broker（消息广播）
        │
        ▼
     Redis :6379          ← 共享在线状态 / 房间缓存 / 分布式锁
        │
        ▼
     MySQL :3306          ← 持久化数据
```

**关键设计决策**：

| 决策点 | 选择 | 原因 |
|--------|------|------|
| WebSocket 负载均衡 | least_conn | 长连接不适合轮询，按连接数分发更均匀 |
| HTTP 负载均衡 | ip_hash | 保证同一客户端会话一致性 |
| STOMP Broker | RabbitMQ（external） | 多实例间消息广播，SimpleBroker 只能在单实例内工作 |
| 在线状态 | Redis | 多实例共享，避免单机内存不一致 |
| 分布式锁 | Redis SET NX EX | 防止多实例并发重复结算同一回合 |

### 8.4 集群验证结果（2026-07-05）

以下验证项已全部通过：

| 验证项 | 结果 | 说明 |
|--------|------|------|
| 双实例启动 (8080/8081) | ✅ | external broker 模式，均成功连接 RabbitMQ |
| Nginx HTTP 代理 | ✅ | `GET /api/actuator/health` → 200 |
| 用户注册 (经 Nginx) | ✅ | `POST /api/users` → 201 |
| 用户查询 (经 Nginx) | ✅ | `GET /api/users/{username}` → 200 |
| WebSocket 握手 | ✅ | HTTP 101 Switching Protocols，正确返回 Sec-WebSocket-Accept |
| SockJS info 端点 | ✅ | `GET /api/ws-battle/info` → 200 |
| RabbitMQ STOMP 连接 | ✅ | 管理界面显示 2 个 STOMP 连接（每个实例一个） |

## 9. Redis 数据模型速查

### 9.1 Key 命名规范

```
campus:{module}:{entity}:{identifier}
```

| 迁移目标 | Redis Key 格式 | 数据结构 | TTL |
|----------|---------------|----------|-----|
| 在线用户集合 | `campus:online:users` | Set | 30min（用户级） |
| 用户→房间映射 | `campus:user:{username}:room` | String | 30min |
| 用户→session 映射 | `campus:user:{username}:session` | String | 30min |
| session→用户映射 | `campus:session:{sessionId}:user` | String | 30min |
| 房间基础状态 | `campus:room:{roomCode}:state` | Hash | 30min |
| 双方作答状态 | `campus:room:{roomCode}:state`（Hash 字段） | Hash | 30min |
| 回合答案缓存 | `campus:room:{roomCode}:answer:{username}` | String (JSON) | 60s |
| 分布式锁 | `campus:room:{roomCode}:lock` | String (SET NX EX) | 10s |

### 9.2 常用 Redis 调试命令

```bash
# 查看所有 Campus 相关 key
redis-cli KEYS "campus:*"

# 查看在线用户
redis-cli SMEMBERS "campus:online:users"

# 查看房间状态
redis-cli HGETALL "campus:room:{roomCode}:state"

# 查看某用户的所在房间
redis-cli GET "campus:user:{username}:room"

# 监控实时命令
redis-cli MONITOR
```

## 10. 后端下一步计划

### 10.1 第一阶段：Redis 状态迁移 ✅ 已完成

> 完成日期：2026-07-05

涉及的改造文件：

- [BattleRoomCacheService](demo/src/main/java/com/campusguess/demo/service/BattleRoomCacheService.java) — 接口定义
- [BattleRoomCacheServiceImpl](demo/src/main/java/com/campusguess/demo/service/impl/BattleRoomCacheServiceImpl.java) — Redis 操作实现
- [OnlineUserServiceImpl](demo/src/main/java/com/campusguess/demo/service/impl/OnlineUserServiceImpl.java) — 在线状态 Redis 化
- [BattleWebSocketController](demo/src/main/java/com/campusguess/demo/controller/BattleWebSocketController.java) — 分布式锁 + 答案缓存 + 清除
- [RedisConfig](demo/src/main/java/com/campusguess/demo/config/RedisConfig.java) — Redis 序列化配置

验证脚本：[test-verify-redis.ps1](test-verify-redis.ps1)

### 10.2 第二阶段：WebSocket 集群化原型验证 ✅ 已完成

> 完成日期：2026-07-05

验证文件：

- [demo/nginx-cluster.conf](demo/nginx-cluster.conf) — Nginx 集群配置
- [demo/start-cluster.bat](demo/start-cluster.bat) — 一键启动脚本

### 10.3 第三阶段：微服务拆分评估

等前两阶段稳定后，再考虑按业务边界拆分用户、游戏核心、记录统计和实时服务。

## 11. 后端分支隔离规则

- 后端任务只在后端分支处理，建议使用 feat/backend-xxx 或 fix/backend-xxx。
- 不要在同一个分支里同时改前端页面和后端实现。
- 如果必须联动，先冻结后端接口，再让前端分支对接。
- 后端只保证接口契约、数据一致性、消息路由和状态可靠性，不负责前端视觉。

## 12. 后端接手优先文件

- [demo/src/main/java/com/campusguess/demo/controller/BattleWebSocketController.java](demo/src/main/java/com/campusguess/demo/controller/BattleWebSocketController.java)
- [demo/src/main/java/com/campusguess/demo/service/impl/BattleServiceImpl.java](demo/src/main/java/com/campusguess/demo/service/impl/BattleServiceImpl.java)
- [demo/src/main/java/com/campusguess/demo/service/impl/OnlineUserServiceImpl.java](demo/src/main/java/com/campusguess/demo/service/impl/OnlineUserServiceImpl.java)
- [demo/src/main/java/com/campusguess/demo/service/BattleRoomCacheService.java](demo/src/main/java/com/campusguess/demo/service/BattleRoomCacheService.java)
- [demo/src/main/java/com/campusguess/demo/service/impl/BattleRoomCacheServiceImpl.java](demo/src/main/java/com/campusguess/demo/service/impl/BattleRoomCacheServiceImpl.java)
- [demo/src/main/java/com/campusguess/demo/config/WebSocketConfig.java](demo/src/main/java/com/campusguess/demo/config/WebSocketConfig.java)
- [demo/src/main/java/com/campusguess/demo/config/RedisConfig.java](demo/src/main/java/com/campusguess/demo/config/RedisConfig.java)
- [demo/src/main/java/com/campusguess/demo/model/entity/BattleRoom.java](demo/src/main/java/com/campusguess/demo/model/entity/BattleRoom.java)
- [demo/src/main/java/com/campusguess/demo/controller/QuestionController.java](demo/src/main/java/com/campusguess/demo/controller/QuestionController.java)
- [demo/src/main/java/com/campusguess/demo/controller/RecordController.java](demo/src/main/java/com/campusguess/demo/controller/RecordController.java)
- [demo/src/main/resources/application.yml](demo/src/main/resources/application.yml)
- [demo/nginx-cluster.conf](demo/nginx-cluster.conf)
- [demo/start-cluster.bat](demo/start-cluster.bat)