# CampusGuessSystem 后端交接文档

本文档是后端接手的唯一参考文档，聚焦服务端架构、接口契约、数据持久化、实时对战状态和集群化部署。前端任务请单独查看前端交接文档，二者分支和职责必须隔离。

## 1. 项目定位

项目是"校园图寻"多人实时对战游戏。当前阶段已完成从单体架构到微服务架构的完整拆分，并实现了 Redis 状态迁移和 WebSocket 集群化部署验证。

最重要的两个后端目标已全部完成：

1. ~~将游戏状态从单机内存迁移到 Redis，解决房间、在线状态、作答状态和临时回合数据在单实例上的局限。~~ **✅ 已完成（2026-07-05）**
2. ~~验证 WebSocket 集群化部署原型，至少完成 Nginx 反向代理 + 多 Spring Boot 实例的协同工作验证。~~ **✅ 已完成（2026-07-05）**
3. ~~按业务边界拆分微服务：用户、题目、记录、社交、对战 + 公共模块 + 网关~~ **✅ 已完成（2026-07-08）**

## 2. 代码结构与技术栈

### 2.1 微服务模块

当前项目已从单体 `demo` 模块拆分为多个独立微服务，采用 Maven 多模块结构：

| 模块 | 端口 | 职责 |
|------|------|------|
| **campus-common** | — | 公共实体（User）、DTO、异常、全局配置 |
| **campus-gateway** | 8080 | Spring Cloud Gateway 统一入口 |
| **campus-user-service** | 8081 | 用户注册、登录（JWT）、积分管理 |
| **campus-question-service** | 8082 | 题库管理（CRUD）、图片标识 |
| **campus-record-service** | 8083 | 答题记录提交与查询 |
| **campus-social-service** | 8084 | 好友添加/接受/拒绝、评论与点赞 |
| **campus-battle-service** | 8085 | 实时对战 WebSocket、房间管理、回合流转 |

每个模块结构一致：

```
campus-{module}-service/
├── src/main/java/com/campusguess/{module}/
│   ├── config/         # Security、Redis 等配置
│   ├── controller/     # HTTP 控制器 / WebSocket 处理器
│   ├── dto/            # 请求/响应 DTO
│   ├── entity/         # JPA 实体
│   ├── repository/     # JPA Repository
│   ├── service/        # 业务接口与实现
│   └── util/           # 工具类
└── src/main/resources/
    └── application.yml
```

### 2.2 技术栈

- Spring Boot 4.0.1 / Spring Cloud
- Java 17
- MySQL 8.0 + Spring Data JPA
- WebSocket 使用原始 WebSocket 协议（JSON 消息格式）
- Redis（在线状态、房间缓存、分布式锁）
- RabbitMQ（外部消息代理，集群模式）
- Spring Security + JWT（各服务独立认证）
- Nginx（反向代理 + 负载均衡，集群模式）

## 3. 运行环境搭建

### 3.1 前置条件

| 软件 | 版本要求 | 用途 |
|------|---------|------|
| JDK | 17+ | 编译与运行 Spring Boot |
| Maven | 3.8+ | 项目构建 |
| Docker | 20.10+ | 运行 RabbitMQ（仅集群模式） |
| MySQL | 8.0+ | 持久化数据 |
| Redis | 7.0+ | 在线状态 / 房间缓存 / 分布式锁 |
| Nginx | 1.24+ | 反向代理与负载均衡（仅集群模式） |

### 3.2 启动 MySQL

确保数据库 `campusguesssystem` 已创建：

```sql
CREATE DATABASE IF NOT EXISTS campusguesssystem CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
```

Docker 方式：

```bash
docker run -d --name mysql-campus `
  -p 3306:3306 `
  -e MYSQL_ROOT_PASSWORD=134679*Abc `
  -e MYSQL_DATABASE=campusguesssystem `
  mysql:8.0
```

### 3.3 启动 Redis

```bash
redis-server
# 默认端口 6379，无需密码
```

验证：`redis-cli PING` → PONG

### 3.4 启动 RabbitMQ（仅集群模式）

```bash
docker run -d --name rabbitmq-campus `
  -p 5672:5672 -p 15672:15672 -p 61613:61613 `
  rabbitmq:3-management
docker exec rabbitmq-campus rabbitmq-plugins enable rabbitmq_stomp
```

### 3.5 安装 Nginx（仅集群模式）

项目根目录 [nginx-cluster.conf](nginx-cluster.conf) 已提供完整集群化 Nginx 配置。

### 3.6 启动所有微服务

**单机模式**（推荐本地开发，无需 RabbitMQ 和 Nginx）：

```bash
# 在项目根目录执行
mvn clean package -DskipTests

# 分别启动各服务（或使用 IDE 启动各 Application 类）
# 端口：8080(网关) 8081(用户) 8082(题目) 8083(记录) 8084(社交) 8085(对战)
```

**各服务 application.yml 关键配置**：

- 所有服务共用同一 MySQL 数据库 `campusguesssystem`
- Redis 连接 `localhost:6379`
- 对战服务 `broker.type=simple`（单机模式），`online-user.store=redis`
- 对战服务端口 8085，WebSocket 端点 `/ws/battle`

## 4. 已实现的后端核心模块

### 4.1 用户与认证（campus-user-service）

- `POST /api/auth/register` — 用户注册
- `POST /api/auth/login` — 用户登录，返回 JWT token
- `GET /api/users/{username}` — 查询用户信息
- `PUT /api/users/{username}/changePoint` — 修改积分
- JWT 认证：各服务通过 Spring Security + JwtAuthenticationFilter 实现无状态认证

### 4.2 题目管理（campus-question-service）

- `POST /api/questions` — 创建题目
- `GET /api/questions` — 分页查询题目列表
- `GET /api/questions/{id}` — 获取题目详情
- `DELETE /api/questions/{id}` — 删除题目
- 题目实体使用 `imageKey` 保存图片标识，前端按图床域名拼接展示

### 4.3 游戏记录（campus-record-service）

- `POST /api/records` — 提交对战记录
- `GET /api/records` — 查询用户记录列表
- `GET /api/records/{id}` — 查看记录详情
- 包含 RecordItem 明细，记录每题坐标、距离、得分

### 4.4 好友与评论（campus-social-service）

- `POST /api/friends/add` — 发送好友请求
- `POST /api/friends/handle` — 接受/拒绝好友请求
- `GET /api/friends/{username}` — 查询好友列表
- `POST /api/comments` — 创建评论
- `GET /api/comments` — 查询评论（支持分页）
- 评论支持点赞功能

### 4.5 实时对战（campus-battle-service）

详见第 5 节。

## 5. 实时对战后端链路

### 5.1 WebSocket 服务端入口

WebSocket 配置在 [WebSocketConfig](campus-battle-service/src/main/java/com/campusguess/battle/config/WebSocketConfig.java)：

- **端点**: `/ws/battle`
- **协议**: 原始 WebSocket（非 STOMP），使用 JSON 消息格式
- **处理器**: [BattleWebSocketHandler](campus-battle-service/src/main/java/com/campusguess/battle/controller/BattleWebSocketHandler.java)
- 支持跨域（`setAllowedOrigins("*")`）

### 5.2 消息协议

客户端发送 JSON 消息，通过 `type` 字段区分消息类型：

| type | 方向 | 说明 |
|------|------|------|
| `login` | 客户端→服务端 | 用户登录，携带 username |
| `invite` | 客户端→服务端 | 发起对战邀请，携带 inviter、invitee、gameType |
| `invite_response` | 客户端→服务端 | 响应邀请，携带 roomCode、accepted、username |
| `answer` | 客户端→服务端 | 提交答案，携带 roomCode、username、lon、lat |
| `next_round` | 客户端→服务端 | 请求下一回合，携带 roomCode、username |
| `heartbeat` | 客户端→服务端 | 心跳，携带 username |

服务端推送的 BattleStateMessage 包含：

| 字段 | 说明 |
|------|------|
| `roomCode` | 房间号 |
| `playerA` / `playerB` | 对战双方用户名 |
| `status` | WAITING / PLAYING / FINISHED |
| `currentRound` | 当前回合数 |
| `questionId` | 当前题目 ID |
| `questionImageUrl` | 题目图片 URL |
| `playerAHealth` / `playerBHealth` | 双方血量 |
| `message` | 提示信息 |
| `winner` | 胜利者（游戏结束时） |

### 5.3 在线状态管理

在线状态由 [OnlineUserServiceRedisImpl](campus-battle-service/src/main/java/com/campusguess/battle/service/impl/OnlineUserServiceRedisImpl.java) 维护，全部存储在 Redis 中：

- 在线用户集合 → Redis Set（`campus:online:users`）
- 用户→房间映射 → Redis String（`campus:user:{username}:room`）
- 用户→sessionId 映射 → Redis String（`campus:user:{username}:session`）
- sessionId→用户映射 → Redis String（`campus:session:{sessionId}:user`）

所有在线状态数据均设置 TTL（30 分钟），多实例共享同一 Redis 实例。

### 5.4 战斗房间与回合流转

战斗房间实体：[BattleRoom](campus-battle-service/src/main/java/com/campusguess/battle/entity/BattleRoom.java)

状态流转：

1. 邀请方创建房间，状态为 WAITING
2. 被邀请方接受后，状态变为 PLAYING，推送 GAME_START
3. 双方提交答案后，先推送 PLAYER_ANSWERED，再计算回合结果
4. 某方血量归零则结束对战并推送 GAME_OVER
5. 否则进入下一回合并推送 NEW_QUESTION

### 5.5 房间缓存层

[BattleRoomCacheServiceImpl](campus-battle-service/src/main/java/com/campusguess/battle/service/impl/BattleRoomCacheServiceImpl.java) 封装所有 Redis 房间操作：

- `cacheRoomState` / `getRoomState` — 房间全量状态缓存（Hash，TTL 30min）
- `updateRoomStatus` — 增量更新作答状态等字段
- `cachePlayerAnswer` — 玩家答案缓存（String JSON，TTL 60s）
- `clearPlayerAnswers` — 清除双方答案缓存
- `tryLockRoom` / `unlockRoom` — 分布式锁（SET NX EX 10s）
- `evictRoom` — 房间结束时清理所有相关 key

## 6. 后端数据存储现状

当前采用"**Redis 实时态** + 数据库持久态"的混合方案。

- 房间和对局过程实时保存到数据库，同时缓存到 Redis Hash
- 在线映射、战斗中状态、作答状态均通过 Redis 管理
- 每回合答案、距离、扣血封装成 BattleRoundRecord，序列化为 JSON 保存
- 对局结束时，BattleServiceImpl 把回合历史转换为 Record 和 RecordItem 落库

## 7. 后端接口契约

### 7.1 HTTP 接口

| 服务 | 路径前缀 | 主要功能 |
|------|---------|---------|
| campus-user-service | `/api/auth`, `/api/users` | 注册、登录、查询、积分 |
| campus-question-service | `/api/questions` | 题目 CRUD |
| campus-record-service | `/api/records` | 记录提交与查询 |
| campus-social-service | `/api/friends`, `/api/comments` | 好友管理、评论 |

统一返回格式：`ApiResponse<T>`（code、message、data）

### 7.2 WebSocket 接口

- 端点：`ws://localhost:8085/ws/battle`
- 消息格式：JSON，通过 `type` 字段路由
- 无需 STOMP 订阅，直接通过 WebSocket 收发 JSON 消息

## 8. 部署模式

### 8.1 单机开发模式

```bash
# 1. 确保 MySQL 和 Redis 已启动
# 2. 在项目根目录构建
mvn clean package -DskipTests

# 3. 分别启动各微服务（IDE 或命令行）
# 对战服务配置 broker.type=simple，online-user.store=redis
```

### 8.2 集群模式

需要 RabbitMQ + Nginx 协同。详见 [nginx-cluster.conf](nginx-cluster.conf)。

### 8.3 集群架构图

```
Browser WebSocket Client
         │
         ▼
    Nginx :80 (least_conn for WS, ip_hash for HTTP)
         │
    ┌────┴────┐
    ▼         ▼
 :8085      :8085'        ← Battle Service 实例
    │         │
    └────┬────┘
         ▼
    RabbitMQ :61613        ← 外部消息代理
         │
         ▼
      Redis :6379          ← 共享在线状态 / 房间缓存 / 分布式锁
         │
         ▼
      MySQL :3306          ← 持久化数据
```

## 9. Redis 数据模型速查

```
campus:online:users                          → Set      (在线用户集合)
campus:user:{username}:room                  → String   (用户→房间)
campus:user:{username}:session               → String   (用户→session)
campus:session:{sessionId}:user              → String   (session→用户)
campus:room:{roomCode}:state                 → Hash     (房间状态)
campus:room:{roomCode}:answer:{username}     → String   (答案缓存, TTL 60s)
campus:room:{roomCode}:lock                  → String   (分布式锁, TTL 10s)
```

## 10. 已完成里程碑

### 第一阶段：Redis 状态迁移 ✅（2026-07-05）

- 在线状态、房间缓存、作答状态、分布式锁全部迁移到 Redis
- 验证脚本：[test-verify-redis.ps1](test-verify-redis.ps1)

### 第二阶段：WebSocket 集群化原型验证 ✅（2026-07-05）

- Nginx 反向代理 + 双 Spring Boot 实例 + RabbitMQ STOMP Broker 协同验证通过
- 验证文件：[nginx-cluster.conf](nginx-cluster.conf)

### 第三阶段：微服务拆分 ✅（2026-07-08）

项目已从单体 `demo` 模块拆分为 7 个独立模块：

| 模块 | 端口 | 状态 |
|------|------|------|
| campus-common | — | ✅ 公共实体、DTO、异常、配置 |
| campus-gateway | 8080 | ✅ Spring Cloud Gateway |
| campus-user-service | 8081 | ✅ 注册、登录(JWT)、积分 |
| campus-question-service | 8082 | ✅ 题目 CRUD |
| campus-record-service | 8083 | ✅ 记录提交与查询 |
| campus-social-service | 8084 | ✅ 好友、评论、点赞 |
| campus-battle-service | 8085 | ✅ 实时对战 WebSocket |

### 第四阶段：全链路功能验证 ✅（2026-07-08）

| 验证项 | 结果 | 说明 |
|--------|------|------|
| 用户注册 | ✅ | POST /api/auth/register |
| 用户登录 | ✅ | POST /api/auth/login 返回 JWT token |
| 题目创建 | ✅ | POST /api/questions 创建成功 |
| 好友添加 | ✅ | POST /api/friends/add 发送/接受好友请求 |
| 评论功能 | ✅ | POST /api/comments 创建评论 |
| 记录提交 | ✅ | POST /api/records 提交答题记录 |
| 积分变更 | ✅ | PUT /api/users/{username}/changePoint |
| 对战 WebSocket | ✅ | ws://localhost:8085/ws/battle 连接正常，login/heartbeat 正常 |

## 11. 后端下一步计划

- WebSocket 协议已从 STOMP 改为原始 WebSocket（JSON），前端需同步适配
- 对战完整流程（邀请→接受→答题→结算→下一回合）需前后端联调验证
- 各服务独立 Security 配置可考虑统一抽取到 Gateway 层
- 评估是否需要引入服务注册与发现（Nacos/Eureka）

## 12. 后端分支隔离规则

- 后端任务只在后端分支处理，建议使用 `feat/backend-xxx` 或 `fix/backend-xxx`
- 不要在同一个分支里同时改前端页面和后端实现
- 如果必须联动，先冻结后端接口，再让前端分支对接
- 后端只保证接口契约、数据一致性、消息路由和状态可靠性

## 13. 后端接手优先文件

- [campus-battle-service/.../controller/BattleWebSocketHandler.java](campus-battle-service/src/main/java/com/campusguess/battle/controller/BattleWebSocketHandler.java)
- [campus-battle-service/.../service/impl/BattleServiceImpl.java](campus-battle-service/src/main/java/com/campusguess/battle/service/impl/BattleServiceImpl.java)
- [campus-battle-service/.../service/impl/OnlineUserServiceRedisImpl.java](campus-battle-service/src/main/java/com/campusguess/battle/service/impl/OnlineUserServiceRedisImpl.java)
- [campus-battle-service/.../service/impl/BattleRoomCacheServiceImpl.java](campus-battle-service/src/main/java/com/campusguess/battle/service/impl/BattleRoomCacheServiceImpl.java)
- [campus-battle-service/.../config/WebSocketConfig.java](campus-battle-service/src/main/java/com/campusguess/battle/config/WebSocketConfig.java)
- [campus-battle-service/.../config/RedisConfig.java](campus-battle-service/src/main/java/com/campusguess/battle/config/RedisConfig.java)
- [campus-battle-service/.../entity/BattleRoom.java](campus-battle-service/src/main/java/com/campusguess/battle/entity/BattleRoom.java)
- [campus-battle-service/.../dto/BattleStateMessage.java](campus-battle-service/src/main/java/com/campusguess/battle/dto/BattleStateMessage.java)
- [campus-battle-service/src/main/resources/application.yml](campus-battle-service/src/main/resources/application.yml)
- [pom.xml](pom.xml) — 父 POM，管理所有模块
- [nginx-cluster.conf](nginx-cluster.conf)
- [test-websocket.html](test-websocket.html) — WebSocket 测试页面