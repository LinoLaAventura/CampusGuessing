# 局域网对战测试指南

## 🌐 网络拓扑

```
电脑A（服务器）          电脑B（客户端）
    ↓                        ↓
    └────── 同一路由器 ──────┘
         (同一WiFi/网段)
```

---

## 📋 准备工作

### 电脑A（运行服务器的电脑）

#### 1. 查找局域网IP地址

**Windows系统：**
```powershell
ipconfig
```

**查找以下格式的IP地址：**
- `192.168.x.x` （家庭/办公室WiFi）
- `10.x.x.x` （某些企业网络）
- `172.16.x.x` 到 `172.31.x.x` （部分企业网络）

**你的IP地址（示例）：**
- 以太网: `172.27.40.74` ✅ 最可能是局域网IP
- VMware虚拟网卡: `192.168.116.1` / `192.168.152.1` ❌ 虚拟机网卡，不用

**Mac/Linux系统：**
```bash
ifconfig
# 或
ip addr show
```

#### 2. 配置防火墙允许8080端口

**Windows防火墙设置（推荐方法1）：**

右键以**管理员身份**运行PowerShell，执行：

```powershell
# 添加入站规则允许8080端口
netsh advfirewall firewall add rule name="CampusGuess 8080" dir=in action=allow protocol=TCP localport=8080

# 验证规则是否添加成功
netsh advfirewall firewall show rule name="CampusGuess 8080"
```

**Windows防火墙设置（图形界面方法2）：**

1. 打开"控制面板" → "系统和安全" → "Windows Defender 防火墙"
2. 点击左侧"高级设置"
3. 点击左侧"入站规则"
4. 点击右侧"新建规则"
5. 选择"端口" → 下一步
6. 选择"TCP"，特定本地端口输入：`8080` → 下一步
7. 选择"允许连接" → 下一步
8. 全部勾选（域、专用、公用）→ 下一步
9. 名称输入：`CampusGuess 8080` → 完成

**Mac防火墙：**

一般Mac默认不会阻止，如果需要：
```bash
# 允许Java应用接收连接
sudo /usr/libexec/ApplicationFirewall/socketfilterfw --add /usr/bin/java
sudo /usr/libexec/ApplicationFirewall/socketfilterfw --unblockapp /usr/bin/java
```

#### 3. 启动Spring Boot应用

确保应用正在运行：
```powershell
# 在demo目录下
mvn spring-boot:run
```

或在IDE中运行 `CampusGuessSystemApplication`

#### 4. 验证服务器可访问

在电脑A上打开浏览器访问：
```
http://localhost:8080/api/battle-demo.html
```

应该能正常打开页面。

---

## 🖥️ 电脑B（客户端电脑）

### 步骤1：确认网络连通性

**在电脑B上测试能否访问电脑A：**

**Windows：**
```powershell
# 替换为电脑A的IP
ping 172.27.40.74

# 测试8080端口是否开放
Test-NetConnection -ComputerName 172.27.40.74 -Port 8080
```

**Mac/Linux：**
```bash
# 测试连通性
ping 172.27.40.74

# 测试端口
telnet 172.27.40.74 8080
# 或
nc -zv 172.27.40.74 8080
```

**期望结果：**
- ping 应该有回复（TTL=xxx）
- 端口测试显示"TcpTestSucceeded: True"或连接成功

**如果ping不通，可能原因：**
1. ❌ 不在同一网络（检查WiFi名称是否相同）
2. ❌ 防火墙阻止了ICMP（可以忽略，只要端口8080通就行）
3. ❌ IP地址错误

**如果端口不通，可能原因：**
1. ❌ 防火墙规则未添加
2. ❌ Spring Boot应用未启动
3. ❌ 应用监听在127.0.0.1而非0.0.0.0

---

## 🎮 完整测试流程

### 电脑A（玩家1 - user1）

1. **打开浏览器**，访问：
   ```
   http://localhost:8080/api/battle-demo.html
   ```

2. **填写连接信息**：
   - 服务器地址：`http://localhost:8080` （本地访问用localhost）
   - 用户名：`user1`

3. **点击"连接"按钮**
   - 等待状态变为"已连接"
   - 日志显示：`[时间] WebSocket已连接: user1`

4. **发起对战邀请**：
   - 邀请用户名输入：`user2`
   - 点击"发起邀请"
   - 日志显示：`[时间] 📨 已发送邀请给: user2，等待对方响应...`

5. **等待对方接受**
   - 当user2接受后，日志会显示：
     ```
     [时间] 状态更新: GAME_START - 对战开始！
     [时间] ✅ 对方接受了邀请！游戏即将开始...
     ```
   - 对战界面自动显示

### 电脑B（玩家2 - user2）

1. **打开浏览器**，访问：
   ```
   http://172.27.40.74:8080/api/battle-demo.html
   ```
   ⚠️ **注意：这里用电脑A的IP，不是localhost！**

2. **填写连接信息**：
   - 服务器地址：`http://172.27.40.74:8080` （用电脑A的IP）
   - 用户名：`user2`

3. **点击"连接"按钮**
   - 等待状态变为"已连接"
   - 日志显示：`[时间] WebSocket已连接: user2`
   - **如果user1已发送邀请，会立即收到通知**：
     ```
     [时间] 📬 收到邀请: user1 邀请你进行对战
     ```
   - 弹出"3. 收到对战邀请"框

4. **接受邀请**：
   - 点击"接受"按钮
   - 日志显示：`[时间] ✅ 已接受邀请，等待游戏开始...`
   - 日志显示：`[时间] 状态更新: GAME_START - 对战开始！`
   - 对战界面自动显示

5. **开始对战！** 🎉

---

## 🎯 对战过程

### 界面说明

**对战信息区域：**
- 房间号：8位唯一代码
- 当前回合数
- 双方血量条（绿色，初始100%）

**题目区域：**
- 图片ID（imageKey）
- 提示信息
- 经度/纬度输入框

### 游戏流程

1. **查看题目**
   - 双方同时看到相同题目
   - 根据图片猜测位置

2. **提交答案**
   - 输入经度（如：113.123456）
   - 输入纬度（如：23.123456）
   - 点击"提交答案"
   - 等待对方作答

3. **对方作答提示**
   - 第一个玩家提交后，另一个玩家会看到：
     ```
     [时间] ⏰ 对方已作答，请尽快作答！倒计时: 30秒
     ```

4. **回合结果**
   - 双方都作答后自动计算
   - 显示双方距离正确答案的距离
   - 距离较远的玩家扣血（10-40点）
   - 血量条实时更新

5. **下一回合**
   - 3秒后自动开始新回合
   - 随机抽取新题目
   - 重复游戏流程

6. **游戏结束**
   - 某方血量归零时游戏结束
   - 弹窗显示获胜者
   - 显示最终血量

---

## 🐛 常见问题排查

### 问题1：电脑B无法访问http://IP:8080

**检查清单：**

1. ✅ 确认电脑A的Spring Boot应用正在运行
   ```powershell
   # 在电脑A执行
   netstat -ano | findstr :8080
   ```
   应该看到类似：`TCP    0.0.0.0:8080    0.0.0.0:0    LISTENING`

2. ✅ 确认防火墙规则
   ```powershell
   # 在电脑A执行
   netsh advfirewall firewall show rule name="CampusGuess 8080"
   ```

3. ✅ 尝试关闭防火墙测试（临时）
   ```powershell
   # 管理员权限
   netsh advfirewall set allprofiles state off
   
   # 测试后记得重新开启
   netsh advfirewall set allprofiles state on
   ```

4. ✅ 检查是否在同一网络
   - 查看WiFi名称是否相同
   - 查看IP地址是否在同一网段（如都是192.168.1.x）

### 问题2：连接成功但收不到邀请

**检查：**
1. 确认用户名输入正确（区分大小写）
2. 打开浏览器开发者工具（F12）查看Console是否有错误
3. 检查消息日志区域是否有WebSocket连接成功的消息

### 问题3：连接显示"Whoops! Lost connection"

**原因：**
- WebSocket连接中断
- 服务器未正确运行
- 网络不稳定

**解决：**
1. 刷新页面重新连接
2. 检查服务器日志是否有异常
3. 检查网络连接

### 问题4：游戏过程中题目显示"undefined"或空白

**原因：**
- 数据库题库为空

**解决：**
1. 确保数据库中有题目数据
2. 题目必须包含：
   - imageKey
   - correctLon（正确经度）
   - correctLat（正确纬度）

---

## 📱 手机测试

如果想用手机测试，确保手机连接同一WiFi：

**手机浏览器访问：**
```
http://172.27.40.74:8080/api/battle-demo.html
```

**配置：**
- 服务器地址：`http://172.27.40.74:8080`
- 用户名：`mobile_user`

---

## 🔐 安全建议

**生产环境注意事项：**

1. ❌ 不要使用 `setAllowedOriginPatterns("*")`
   - 改为指定域名：`.setAllowedOrigins("https://yourdomain.com")`

2. ❌ 不要在公网暴露开发环境
   - 使用VPN或内网穿透工具
   - 或部署到正式服务器

3. ✅ 添加用户认证
   - 集成JWT token验证
   - 验证用户权限

4. ✅ 使用HTTPS
   - 申请SSL证书
   - 配置WSS协议（WebSocket Secure）

---

## 📝 测试记录模板

**测试日期：** 2026-01-12

**网络环境：**
- 电脑A IP: 172.27.40.74
- 电脑B IP: 192.168.1.105
- 路由器型号: XXX
- WiFi名称: XXX

**测试结果：**
- [ ] 连接测试通过
- [ ] 邀请发送成功
- [ ] 邀请接收成功
- [ ] 游戏流程正常
- [ ] 血量扣除正确
- [ ] 游戏结束正常

**问题记录：**
（记录遇到的问题和解决方法）

---

## 🎓 进阶：内网穿透（可选）

如果想让不在同一局域网的朋友也能测试，可以使用内网穿透工具：

**推荐工具：**

1. **ngrok** (https://ngrok.com)
   ```bash
   ngrok http 8080
   ```
   会生成一个公网地址，如：`https://xxxx.ngrok.io`

2. **frp** (https://github.com/fatedier/frp)
   - 需要一台有公网IP的服务器

3. **cpolar** (https://www.cpolar.com)
   - 国内访问速度较好
   ```bash
   cpolar http 8080
   ```

使用内网穿透后，朋友只需访问生成的公网地址即可！

---

祝测试顺利！🚀
