# CampusGuessSystem 前端交接文档

本文档是前端接手的唯一参考文档，聚焦页面、状态管理、WebSocket 客户端接入、对战交互和前后端契约。后端任务请单独查看后端交接文档，二者必须分支隔离，互不干扰。

## 1. 项目定位

项目是"校园图寻"多人实时对战游戏。前端当前最重要的任务不是单纯做页面，而是稳定承接后端的实时对战协议，让邀请、接受、作答、结算和重连流程保持一致。

前端需要和后端严格分离：

- 前端分支只做页面、组件、状态和 WebSocket 客户端。
- 不要在前端分支里直接改后端服务逻辑。
- 如果接口要改，先在后端分支冻结契约，再由前端对接。

## 2. 开发环境搭建

### 2.1 前置依赖

| 软件 | 版本要求 | 用途 |
|------|---------|------|
| Node.js | 18+ | 前端构建工具链（如使用 React/Vue 独立项目） |
| 浏览器 | Chrome/Edge 最新版 | 运行与调试 |
| JDK | 17+ | 编译与运行后端 |
| Maven | 3.8+ | 后端构建 |
| Docker | 20.10+ | 运行 RabbitMQ（仅集群模式需要） |
| MySQL | 8.0+ | 数据库（可本地安装或 Docker） |
| Redis | 7.0+ | 在线状态 / 房间缓存 |
| Nginx | 1.24+ | 反向代理（仅集群模式需要） |

### 2.2 启动 MySQL

确保数据库 `campusguesssystem` 已创建：

```sql
CREATE DATABASE IF NOT EXISTS campusguesssystem CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
```

Docker 方式（如果本地没有 MySQL）：

```bash
docker run -d --name mysql-campus -p 3306:3306 -e MYSQL_ROOT_PASSWORD=134679*Abc -e MYSQL_DATABASE=campusguesssystem mysql:8.0
```

### 2.3 启动 Redis

Windows 本地安装：

```bash
redis-server
```

Docker 方式：

```bash
docker run -d --name redis-campus -p 6379:6379 redis:7-alpine
```

验证：

```bash
redis-cli PING
# 预期输出: PONG
```

### 2.4 启动 RabbitMQ（仅集群模式需要）

单机模式不需要 RabbitMQ。集群模式下需要它作为外部 STOMP Broker：

```bash
docker run -d --name rabbitmq-campus -p 5672:5672 -p 15672:15672 -p 61613:61613 rabbitmq:3-management
docker exec rabbitmq-campus rabbitmq-plugins enable rabbitmq_stomp
```

管理界面：http://localhost:15672（guest/guest）

端口说明：

| 端口 | 用途 |
|------|------|
| 5672 | AMQP 协议 |
| 15672 | 管理界面 |
| 61613 | STOMP 协议（Spring 连接用） |

### 2.5 安装 Nginx（仅集群模式需要）

下载 [Nginx for Windows](https://nginx.org/en/download.html)，解压到 `D:\nginx\nginx-1.30.3\`（或其他路径，需同步修改 `start-cluster.bat` 中的路径）。

项目已提供集群化 Nginx 配置文件 [demo/nginx-cluster.conf](demo/nginx-cluster.conf)，使用前需复制到 Nginx 目录：

```bash
copy /Y demo\nginx-cluster.conf D:\nginx\nginx-1.30.3\conf\nginx-cluster.conf
```

### 2.6 启动后端

**单机模式**（推荐日常开发，无需 RabbitMQ 和 Nginx）：

```bash
cd demo
mvnw.cmd spring-boot:run
```

此时后端运行在 `http://localhost:8080/api`，WebSocket 端点 `ws://localhost:8080/api/ws-battle`。

**集群模式**（验证多实例消息分发时使用）：

```bash
# 使用一键启动脚本（自动检查环境、构建、启动双实例 + Nginx）
demo\start-cluster.bat
```

集群模式通过 Nginx 访问：`http://localhost/api`，WebSocket 端点 `ws://localhost/api/ws-battle`。

停止集群：

```bash
taskkill /fi "WINDOWTITLE eq CampusGuessing-8080" /f
taskkill /fi "WINDOWTITLE eq CampusGuessing-8081" /f
taskkill /IM nginx.exe /f
docker stop rabbitmq-campus
```

### 2.7 调试页面

项目内置的调试页面 [demo/src/main/resources/static/battle-demo.html](demo/src/main/resources/static/battle-demo.html) 可以直接使用：

- 单机模式：`http://localhost:8080/battle-demo.html`
- 集群模式：`http://localhost/battle-demo.html`（通过 Nginx 代理）

## 3. 前端需要关注的核心场景

### 3.1 WebSocket 对战页面

当前仓库中的 [demo/src/main/resources/static/battle-demo.html](demo/src/main/resources/static/battle-demo.html) 是一个可用于验证实时对战流程的示例页面。它展示了前端应当具备的核心能力：

- 连接 WebSocket
- 以 username 作为身份参数
- 订阅邀请消息
- 订阅对战状态消息
- 发起邀请、接受邀请、拒绝邀请
- 提交答案和退出对战
- 展示回合结果和游戏结束状态

### 3.2 前端状态要素

前端页面至少要维护以下状态：

- 当前用户名
- 当前房间号
- 当前是否在对战中
- 当前回合数
- 双方血量
- 当前题目信息
- 邀请弹窗状态
- 回合结果展示状态
- 消息日志

## 4. WebSocket 接入约定

### 4.1 连接方式

**单机模式**（开发环境）：

```
ws://localhost:8080/api/ws-battle?username={用户名}
```

**集群模式**（通过 Nginx）：

```
ws://localhost/api/ws-battle?username={用户名}
```

要点：

- username 必须随 URL 参数传入。
- 当前支持 SockJS 降级。
- 连接成功后，前端应订阅个人消息队列并保持唯一用户名登录。
- 集群模式下连接 Nginx 端口 80，Nginx 会自动将 WebSocket 请求代理到后端实例。

### 4.2 订阅路径

- 对战邀请订阅：/user/queue/battle/invite
- 对战状态订阅：/user/queue/battle/state

### 4.3 发送路径

- 邀请：/app/battle/invite
- 响应邀请：/app/battle/respond
- 提交答案：/app/battle/answer
- 主动退出：/app/battle/quit

## 5. 前后端消息协议

前端必须能识别并处理以下消息类型：

| 消息类型 | 方向 | 说明 |
|----------|------|------|
| INVITE | 服务端→客户端 | 收到对战邀请 |
| INVITE_ACCEPTED | 服务端→客户端 | 邀请被接受 |
| INVITE_REJECTED | 服务端→客户端 | 邀请被拒绝 |
| GAME_START | 服务端→客户端 | 游戏开始 |
| NEW_QUESTION | 服务端→客户端 | 新回合题目 |
| PLAYER_ANSWERED | 服务端→客户端 | 对方已作答 |
| ROUND_RESULT | 服务端→客户端 | 回合结果 |
| GAME_OVER | 服务端→客户端 | 游戏结束 |

处理原则：

- 收到 INVITE 时，弹出邀请卡片，允许接受或拒绝。
- 收到 GAME_START 或 NEW_QUESTION 时，刷新房间、题目和血量，并打开对战面板。
- 收到 PLAYER_ANSWERED 时，只展示"等待对方"状态，不要提前结算。
- 收到 ROUND_RESULT 时，更新双方血量和回合结果。
- 收到 GAME_OVER 时，关闭对战面板并清空当前对局状态。

## 6. HTTP API 速查

前端页面除了 WebSocket 外，还需要调用以下 HTTP 接口：

| 接口 | 方法 | 路径 | 用途 |
|------|------|------|------|
| 用户注册 | POST | `/api/users` | 注册新用户 |
| 用户查询 | GET | `/api/users/{username}` | 获取用户信息 |
| 修改积分 | PUT | `/api/users/{username}/changePoint` | 修改用户积分 |
| 题目列表 | GET | `/api/questions` | 获取题目（支持分页） |
| 题目详情 | GET | `/api/questions/{id}` | 获取单题详情 |
| 对战记录 | GET | `/api/records` | 查询用户对战记录 |

所有接口统一返回格式：

```json
{
  "code": 200,
  "message": "操作成功",
  "data": { ... }
}
```

## 7. 前端页面职责建议

如果前端代码在独立分支或独立仓库中，建议至少拆成下面几类页面或组件：

- 登录 / 用户身份输入
- 邀请列表或邀请弹窗
- 对战面板
- 题目展示区
- 答案提交区
- 回合结果区
- 消息日志区

推荐的交互顺序：

1. 用户输入 username 并连接。
2. 发起或接收邀请。
3. 进入战斗面板并显示题目。
4. 提交答案，等待回合结果。
5. 处理下一回合或游戏结束。

## 8. 前端当前要注意的约束

- 同一用户名不能在多个页面同时连接，否则会引起消息丢失或状态错乱。
- 连接失败时需要提示用户并允许重试。
- 对战中禁止重复发起邀请。
- 退出对战后要清空当前房间信息，避免误提交答案。
- 如果后端返回 INVITE_REJECTED，要在界面上明确展示拒绝原因。

## 9. 前端与后端联调关注点

### 9.1 接口冻结

前端分支只能依赖稳定的后端契约。对战前后端最重要的是消息类型、字段命名和路径不要频繁变动。

### 9.2 页面状态同步

前端必须以服务端消息为准更新 UI，而不是依赖本地乐观假设：

- 房间号以服务端返回值为准。
- 回合结束以 ROUND_RESULT 或 GAME_OVER 为准。
- 下一回合以 NEW_QUESTION 为准。

### 9.3 调试页面

[demo/src/main/resources/static/battle-demo.html](demo/src/main/resources/static/battle-demo.html) 可作为联调时的参考实现，尤其适合验证双标签页对战流程。

## 10. 后端当前状态（已完成里程碑）

后端已完成的改造，前端无需关心实现细节，但需了解其对联调的影响：

### 10.1 Redis 状态迁移（2026-07-05 完成）

在线状态、房间缓存、作答状态、分布式锁均已从单机内存迁移到 Redis，多实例共享状态。对前端的影响：

- 后端重启后在线状态不丢失（Redis 持久化）
- 多实例部署时，不同用户连接到不同后端实例也能正常对战
- 分布式锁防止并发重复结算，回合结果更可靠

### 10.2 WebSocket 集群化原型验证（2026-07-05 完成）

已通过 Nginx 反向代理 + 双 Spring Boot 实例 + RabbitMQ STOMP Broker 的协同工作验证。验证结果：

| 验证项 | 结果 | 说明 |
|--------|------|------|
| 双实例启动 (8080/8081) | ✅ | external broker 模式，均成功连接 RabbitMQ |
| Nginx HTTP 代理 | ✅ | `GET /api/actuator/health` → 200 |
| 用户注册 (经 Nginx) | ✅ | `POST /api/users` → 201 |
| 用户查询 (经 Nginx) | ✅ | `GET /api/users/{username}` → 200 |
| WebSocket 握手 | ✅ | HTTP 101 Switching Protocols，正确返回 Sec-WebSocket-Accept |
| SockJS info 端点 | ✅ | `GET /api/ws-battle/info` → 200 |
| RabbitMQ STOMP 连接 | ✅ | 管理界面显示 2 个 STOMP 连接（每个实例一个） |

集群架构图：

```
Browser STOMP Client
        │
        ▼
   Nginx :80 (least_conn for WS, ip_hash for HTTP)
        │
   ┌────┴────┐
   ▼         ▼
:8080      :8081         ← Spring Boot 实例
   │         │
   └────┬────┘
        ▼
RabbitMQ STOMP :61613    ← 外部 STOMP Broker（消息广播）
        │
        ▼
     Redis :6379          ← 共享在线状态 / 房间缓存 / 分布式锁
        │
        ▼
     MySQL :3306          ← 持久化数据
```

### 10.3 后端下一步

第三阶段将评估按业务边界（用户、游戏核心、记录统计、实时服务）进行微服务拆分。

## 11. 前端下一步计划

### 11.1 第一阶段：对齐实时对战页面

优先把对战页面的连接、邀请、作答、结果展示做稳定，保证和后端当前消息协议一致。

### 11.2 第二阶段：补齐页面结构

如果前端分支有独立 React 代码，建议优先补：

- 对战首页
- 邀请处理页
- 对战中页
- 游戏结果页
- 记录查看页

### 11.3 第三阶段：体验优化

在协议稳定后，再做重连提示、状态动画、错误提示和布局优化。

## 12. 前端分支隔离规则

- 前端任务只在前端分支处理，建议使用 feat/frontend-xxx 或 fix/frontend-xxx。
- 不要在前端分支里修改后端 controller、service 或 repository。
- 接口有变更时，先让后端分支完成冻结，再在前端分支适配。
- 前端只负责界面、状态和客户端连接，不负责服务端状态同步实现。

## 13. 前端接手优先文件

- [demo/src/main/resources/static/battle-demo.html](demo/src/main/resources/static/battle-demo.html) — 实时对战调试页面
- [demo/start-cluster.bat](demo/start-cluster.bat) — 集群一键启动脚本（含环境检查）
- [demo/nginx-cluster.conf](demo/nginx-cluster.conf) — Nginx 集群配置（WebSocket 代理、负载均衡策略）