# CampusGuessSystem 前端交接文档

本文档是前端接手的唯一参考文档，聚焦页面、状态管理、WebSocket 客户端接入、对战交互和前后端契约。后端任务请单独查看后端交接文档，二者必须分支隔离，互不干扰。

## 1. 项目定位

项目是"校园图寻"多人实时对战游戏。前端当前最重要的任务不是单纯做页面，而是稳定承接后端的实时对战协议，让邀请、接受、作答、结算和重连流程保持一致。

前端需要和后端严格分离：

- 前端分支只做页面、组件、状态和 WebSocket 客户端。
- 不要在前端分支里直接改后端服务逻辑。
- 如果接口要改，先在后端分支冻结契约，再由前端对接。

## 2. 开发环境搭建

### 2.1 前置依赖

| 软件 | 版本要求 | 用途 |
|------|---------|------|
| Node.js | 18+ | 前端构建工具链（如使用 React/Vue 独立项目） |
| 浏览器 | Chrome/Edge 最新版 | 运行与调试 |
| JDK | 17+ | 编译与运行后端 |
| Maven | 3.8+ | 后端构建 |
| Docker | 20.10+ | 运行 RabbitMQ（仅集群模式需要） |
| MySQL | 8.0+ | 数据库（可本地安装或 Docker） |
| Redis | 7.0+ | 在线状态 / 房间缓存 |
| Nginx | 1.24+ | 反向代理（仅集群模式需要） |

### 2.2 启动 MySQL

确保数据库 `campusguesssystem` 已创建：

```sql
CREATE DATABASE IF NOT EXISTS campusguesssystem CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
```

Docker 方式（如果本地没有 MySQL）：

```bash
docker run -d --name mysql-campus -p 3306:3306 -e MYSQL_ROOT_PASSWORD=134679*Abc -e MYSQL_DATABASE=campusguesssystem mysql:8.0
```

### 2.3 启动 Redis

Windows 本地安装：

```bash
redis-server
```

Docker 方式：

```bash
docker run -d --name redis-campus -p 6379:6379 redis:7-alpine
```

验证：

```bash
redis-cli PING
# 预期输出: PONG
```

### 2.4 启动 RabbitMQ（仅集群模式需要）

单机模式不需要 RabbitMQ。集群模式下需要它作为外部消息代理：

```bash
docker run -d --name rabbitmq-campus -p 5672:5672 -p 15672:15672 -p 61613:61613 rabbitmq:3-management
docker exec rabbitmq-campus rabbitmq-plugins enable rabbitmq_stomp
```

管理界面：http://localhost:15672（guest/guest）

端口说明：

| 端口 | 用途 |
|------|------|
| 5672 | AMQP 协议 |
| 15672 | 管理界面 |
| 61613 | STOMP 协议（集群模式用） |

### 2.5 安装 Nginx（仅集群模式需要）

下载 [Nginx for Windows](https://nginx.org/en/download.html)，解压到 `D:\nginx\nginx-1.30.3\`（或其他路径，需同步修改 `start-cluster.bat` 中的路径）。

项目已提供集群化 Nginx 配置文件 [nginx-cluster.conf](nginx-cluster.conf)。

### 2.6 启动后端

当前项目已拆分为微服务架构，各服务运行在不同端口：

| 服务 | 端口 | 职责 |
|------|------|------|
| campus-gateway | 8080 | 统一网关 |
| campus-user-service | 8081 | 用户注册、登录、积分 |
| campus-question-service | 8082 | 题库管理 |
| campus-record-service | 8083 | 答题记录 |
| campus-social-service | 8084 | 好友、评论 |
| campus-battle-service | 8085 | 实时对战 WebSocket |

**单机模式**（推荐日常开发，无需 RabbitMQ 和 Nginx）：

```bash
# 在项目根目录构建
mvn clean package -DskipTests

# 分别启动各服务（IDE 或命令行）
# 前置条件：MySQL 和 Redis 已启动
```

单机模式下对战服务自动使用 `broker.type=simple`，直接通过 WebSocket 通信，无需 RabbitMQ。

**集群模式**（验证多实例消息分发时使用）：

```bash
# 使用一键启动脚本（自动检查环境、构建、启动双实例 + Nginx）
demo\start-cluster.bat
```

集群模式通过 Nginx 访问：`http://localhost/api`。

### 2.7 WebSocket 测试页面

项目根目录 [test-websocket.html](test-websocket.html) 可直接用于测试 WebSocket 连接和消息收发：

- 在浏览器中打开 `file:///d:/code/CampusGuessing/test-websocket.html`
- 输入对战服务地址 `ws://localhost:8085/ws/battle`
- 点击"连接"按钮建立 WebSocket 连接
- 点击"登录"发送 login 消息
- 可手动发送 invite、answer、heartbeat 等消息

## 3. 前端需要关注的核心场景

### 3.1 WebSocket 对战页面

前端应当具备的核心能力：

- 连接 WebSocket（`ws://localhost:8085/ws/battle`）
- 以 username 作为身份标识，发送 login 消息
- 接收邀请消息并弹出邀请弹窗
- 接收对战状态消息并更新 UI
- 发起邀请、接受邀请、拒绝邀请
- 在地图上点击坐标提交答案
- 展示回合结果和游戏结束状态
- 支持心跳保活

### 3.2 前端状态要素

前端页面至少要维护以下状态：

- 当前用户名
- 当前房间号
- 当前是否在对战中
- 当前回合数
- 双方血量
- 当前题目信息（图片 URL、题目 ID）
- 邀请弹窗状态
- 回合结果展示状态
- 消息日志

## 4. WebSocket 接入约定

### 4.1 连接方式

**单机模式**（开发环境）：

```
ws://localhost:8085/ws/battle
```

**集群模式**（通过 Nginx）：

```
ws://localhost/ws/battle
```

要点：

- 使用原始 WebSocket 协议（非 STOMP），消息格式为 JSON
- 连接成功后，客户端需先发送 login 消息进行身份注册
- 不需要订阅路径，所有消息通过同一 WebSocket 连接双向收发
- 集群模式下连接 Nginx 端口 80，Nginx 会自动将 WebSocket 请求代理到后端实例

### 4.2 消息协议

**客户端→服务端**（发送）：

| type | 字段 | 说明 |
|------|------|------|
| `login` | `username` | 用户登录注册 |
| `invite` | `inviter`, `invitee`, `gameType` | 发起对战邀请 |
| `invite_response` | `roomCode`, `accepted`, `username` | 接受/拒绝邀请 |
| `answer` | `roomCode`, `username`, `lon`, `lat` | 提交答案坐标 |
| `next_round` | `roomCode`, `username` | 请求下一回合 |
| `heartbeat` | `username` | 心跳保活 |

**服务端→客户端**（接收）：

服务端通过 BattleStateMessage 推送状态，关键字段：

| 字段 | 类型 | 说明 |
|------|------|------|
| `type` | String | 消息类型 |
| `roomCode` | String | 房间号 |
| `playerA` / `playerB` | String | 对战双方用户名 |
| `status` | String | WAITING / PLAYING / FINISHED |
| `currentRound` | Integer | 当前回合数 |
| `questionId` | Long | 当前题目 ID |
| `questionImageUrl` | String | 题目图片 URL |
| `playerAHealth` / `playerBHealth` | Integer | 双方血量 |
| `message` | String | 提示信息 |
| `winner` | String | 胜利者（游戏结束时） |

处理原则：

- 收到邀请消息时，弹出邀请卡片，允许接受或拒绝
- 收到 GAME_START 或 NEW_QUESTION 时，刷新房间、题目和血量，并打开对战面板
- 收到 PLAYER_ANSWERED 时，只展示"等待对方"状态，不要提前结算
- 收到 ROUND_RESULT 时，更新双方血量和回合结果
- 收到 GAME_OVER 时，关闭对战面板并清空当前对局状态

## 5. HTTP API 速查

前端页面除了 WebSocket 外，还需要调用以下 HTTP 接口（可通过 Gateway 8080 统一访问，或直连各服务）：

| 接口 | 方法 | 路径 | 服务 | 用途 |
|------|------|------|------|------|
| 用户注册 | POST | `/api/auth/register` | user-service (8081) | 注册新用户 |
| 用户登录 | POST | `/api/auth/login` | user-service (8081) | 登录获取 JWT token |
| 用户查询 | GET | `/api/users/{username}` | user-service (8081) | 获取用户信息 |
| 修改积分 | PUT | `/api/users/{username}/changePoint` | user-service (8081) | 修改用户积分 |
| 题目列表 | GET | `/api/questions` | question-service (8082) | 获取题目（支持分页） |
| 题目详情 | GET | `/api/questions/{id}` | question-service (8082) | 获取单题详情 |
| 创建题目 | POST | `/api/questions` | question-service (8082) | 创建新题目 |
| 对战记录 | GET | `/api/records` | record-service (8083) | 查询用户对战记录 |
| 提交记录 | POST | `/api/records` | record-service (8083) | 提交对战记录 |
| 添加好友 | POST | `/api/friends/add` | social-service (8084) | 发送好友请求 |
| 处理好友请求 | POST | `/api/friends/handle` | social-service (8084) | 接受/拒绝好友请求 |
| 好友列表 | GET | `/api/friends/{username}` | social-service (8084) | 查询好友列表 |
| 创建评论 | POST | `/api/comments` | social-service (8084) | 发表评论 |
| 查询评论 | GET | `/api/comments` | social-service (8084) | 查询评论列表 |

所有接口统一返回格式：

```json
{
  "code": 200,
  "message": "操作成功",
  "data": { ... }
}
```

## 6. 前端页面职责建议

如果前端代码在独立分支或独立仓库中，建议至少拆成下面几类页面或组件：

- 登录 / 用户身份输入
- 邀请列表或邀请弹窗
- 对战面板
- 题目展示区（地图 + 图片）
- 答案提交区（坐标选择）
- 回合结果区
- 消息日志区

推荐的交互顺序：

1. 用户输入 username 并登录（HTTP + WebSocket login）
2. 发起或接收邀请
3. 进入战斗面板并显示题目
4. 在地图上点击坐标提交答案
5. 等待回合结果
6. 处理下一回合或游戏结束

## 7. 前端当前要注意的约束

- 同一用户名不能在多个页面同时连接，否则会引起消息丢失或状态错乱
- 连接失败时需要提示用户并允许重试
- 对战中禁止重复发起邀请
- 退出对战后要清空当前房间信息，避免误提交答案
- 如果后端返回邀请被拒绝，要在界面上明确展示拒绝原因
- 所有 HTTP 请求需携带 JWT token（登录后获取）
- WebSocket 连接建立后必须先发送 login 消息

## 8. 前端现有代码改造清单（必读）

当前项目中的 `demo/src/main/resources/static/battle-demo.html` 仍使用旧 STOMP 协议，**必须按以下清单逐项改造**才能对接新后端。

### 8.1 移除旧依赖，替换为原生 WebSocket

**删除：**
```html
<!-- 旧：SockJS + STOMP.js -->
<script src="https://cdn.jsdelivr.net/npm/sockjs-client@1/dist/sockjs.min.js"></script>
<script src="https://cdn.jsdelivr.net/npm/stompjs@2.3.3/lib/stomp.min.js"></script>
```

**替换为：** 无需任何外部库，浏览器原生 `WebSocket` 即可。

### 8.2 连接方式变更

**旧代码（STOMP）：**
```javascript
var socket = new SockJS('/api/ws-battle');
var stompClient = Stomp.over(socket);
stompClient.connect({}, function (frame) {
    // 订阅路径
    stompClient.subscribe('/user/queue/battle/invite', onInvite);
    stompClient.subscribe('/user/queue/battle/state', onState);
});
```

**新代码（原生 WebSocket）：**
```javascript
var ws = new WebSocket('ws://localhost:8085/ws/battle');  // 单机模式
// 或集群模式: ws://localhost/ws/battle

ws.onopen = function () {
    // 连接成功后必须先发送 login
    ws.send(JSON.stringify({ type: 'login', username: currentUser }));
};

ws.onmessage = function (event) {
    var msg = JSON.parse(event.data);
    switch (msg.type) {
        case 'INVITE':       onInvite(msg); break;
        case 'BATTLE_STATE': onState(msg); break;
        case 'GAME_OVER':    onGameOver(msg); break;
        // ...
    }
};
```

### 8.3 发送消息方式变更

**旧代码：**
```javascript
stompClient.send('/app/battle/invite', {}, JSON.stringify({ inviter: 'A', invitee: 'B' }));
stompClient.send('/app/battle/answer', {}, JSON.stringify({ roomCode: 'X', lon: 1.0, lat: 2.0 }));
```

**新代码：**
```javascript
ws.send(JSON.stringify({ type: 'invite', inviter: 'A', invitee: 'B', gameType: 'NORMAL' }));
ws.send(JSON.stringify({ type: 'answer', roomCode: 'X', username: 'A', lon: 1.0, lat: 2.0 }));
ws.send(JSON.stringify({ type: 'heartbeat', username: 'A' }));
ws.send(JSON.stringify({ type: 'invite_response', roomCode: 'X', accepted: true, username: 'A' }));
ws.send(JSON.stringify({ type: 'next_round', roomCode: 'X', username: 'A' }));
```

### 8.4 消息路由方式变更

**旧方式：** 按订阅路径分发（`/user/queue/battle/invite` vs `/user/queue/battle/state`）

**新方式：** 统一在 `onmessage` 中按 `type` 字段分发：

```javascript
ws.onmessage = function (event) {
    var msg = JSON.parse(event.data);
    switch (msg.type) {
        case 'INVITE':
            // 弹出邀请弹窗，显示 msg.inviter
            showInviteDialog(msg);
            break;
        case 'BATTLE_STATE':
            // 更新房间状态、血量、回合、题目
            updateBattleUI(msg);
            break;
        case 'GAME_OVER':
            // 展示结算，清空房间状态
            showGameOver(msg);
            break;
        case 'ERROR':
            showError(msg.message);
            break;
    }
};
```

### 8.5 连接地址变更

| 模式 | 旧地址 | 新地址 |
|------|--------|--------|
| 单机 | `ws://localhost:8080/api/ws-battle` 或 `/api/ws-battle` | `ws://localhost:8085/ws/battle` |
| 集群 | `ws://localhost/api/ws-battle` | `ws://localhost/ws/battle` |

### 8.6 HTTP 请求需携带 JWT Token

微服务架构下各服务开启了 Spring Security，HTTP 请求需携带 JWT：

```javascript
fetch('/api/auth/login', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({ username: 'xxx', password: 'xxx' })
})
.then(res => res.json())
.then(data => {
    var token = data.data.token;  // 保存 token
    // 后续请求携带
    fetch('/api/questions', {
        headers: { 'Authorization': 'Bearer ' + token }
    });
});
```

### 8.7 改造对照总结

| 改造项 | 旧 | 新 |
|--------|----|----|
| WebSocket 库 | SockJS + STOMP.js | 无需库，原生 WebSocket |
| 连接代码 | `new SockJS()` + `Stomp.over()` | `new WebSocket(url)` |
| 发送消息 | `stompClient.send(path, headers, body)` | `ws.send(JSON.stringify({type, ...}))` |
| 接收消息 | 多个 `subscribe` 回调 | 单一 `onmessage`，按 `type` switch |
| 身份注册 | STOMP connect 头 | 连接后发送 `{type:"login", username}` |
| 心跳 | SockJS 自动 | 客户端定时发送 `{type:"heartbeat"}` |
| HTTP 认证 | 无 | 登录后携带 `Authorization: Bearer <token>` |

### 8.8 参考实现

[test-websocket.html](test-websocket.html) 已按新协议完整实现，包括连接、login、消息收发、心跳保活，可直接作为改造模板。

---

## 9. 前端与后端联调关注点

### 9.1 接口冻结

前端分支只能依赖稳定的后端契约。对战前后端最重要的是消息类型、字段命名和路径不要频繁变动。

### 9.2 页面状态同步

前端必须以服务端消息为准更新 UI，而不是依赖本地乐观假设：

- 房间号以服务端返回值为准
- 回合结束以 ROUND_RESULT 或 GAME_OVER 为准
- 下一回合以 NEW_QUESTION 为准

### 9.3 协议变更说明（重要）

**当前协议已从 STOMP 改为原始 WebSocket（JSON）**，前端需要注意：

- 不再使用 STOMP 订阅路径（如 `/user/queue/battle/invite`、`/app/battle/invite`）
- 不再使用 SockJS
- 所有消息通过同一 WebSocket 连接以 JSON 格式收发
- 通过 `type` 字段区分消息类型并路由到对应处理逻辑

### 9.4 测试页面

[test-websocket.html](test-websocket.html) 可作为联调时的参考实现，尤其适合验证 WebSocket 连接和消息收发。

## 10. 后端当前状态（已完成里程碑）

后端已完成的改造，前端无需关心实现细节，但需了解其对联调的影响：

### 9.1 微服务拆分（2026-07-08 完成）

项目已从单体 `demo` 模块拆分为 7 个独立模块：

| 模块 | 端口 | 状态 |
|------|------|------|
| campus-common | — | ✅ 公共实体、DTO、异常、配置 |
| campus-gateway | 8080 | ✅ Spring Cloud Gateway |
| campus-user-service | 8081 | ✅ 注册、登录(JWT)、积分 |
| campus-question-service | 8082 | ✅ 题目 CRUD |
| campus-record-service | 8083 | ✅ 记录提交与查询 |
| campus-social-service | 8084 | ✅ 好友、评论、点赞 |
| campus-battle-service | 8085 | ✅ 实时对战 WebSocket |

### 9.2 Redis 状态迁移（2026-07-05 完成）

在线状态、房间缓存、作答状态、分布式锁均已从单机内存迁移到 Redis，多实例共享状态。对前端的影响：

- 后端重启后在线状态不丢失（Redis 持久化）
- 多实例部署时，不同用户连接到不同后端实例也能正常对战
- 分布式锁防止并发重复结算，回合结果更可靠

### 9.3 WebSocket 集群化原型验证（2026-07-05 完成）

已通过 Nginx 反向代理 + 双 Spring Boot 实例 + RabbitMQ STOMP Broker 的协同工作验证。

### 9.4 全链路功能验证（2026-07-08 完成）

| 验证项 | 结果 | 说明 |
|--------|------|------|
| 用户注册 | ✅ | POST /api/auth/register |
| 用户登录 | ✅ | POST /api/auth/login 返回 JWT token |
| 题目创建 | ✅ | POST /api/questions 创建成功 |
| 好友添加 | ✅ | POST /api/friends/add 发送/接受好友请求 |
| 评论功能 | ✅ | POST /api/comments 创建评论 |
| 记录提交 | ✅ | POST /api/records 提交答题记录 |
| 积分变更 | ✅ | PUT /api/users/{username}/changePoint |
| 对战 WebSocket | ✅ | ws://localhost:8085/ws/battle 连接正常，login/heartbeat 正常 |

集群架构图：

```
Browser WebSocket Client
         │
         ▼
    Nginx :80 (least_conn for WS, ip_hash for HTTP)
         │
    ┌────┴────┐
    ▼         ▼
 :8085      :8085'        ← Battle Service 实例
    │         │
    └────┬────┘
         ▼
    RabbitMQ :61613        ← 外部消息代理（集群模式）
         │
         ▼
      Redis :6379          ← 共享在线状态 / 房间缓存 / 分布式锁
         │
         ▼
      MySQL :3306          ← 持久化数据
```

## 11. 前端下一步计划

### 10.1 第一阶段：对齐实时对战页面

优先把对战页面的连接、邀请、作答、结果展示做稳定，保证和后端当前原始 WebSocket（JSON）协议一致。

### 10.2 第二阶段：补齐页面结构

如果前端分支有独立 React 代码，建议优先补：

- 对战首页
- 邀请处理页
- 对战中页
- 游戏结果页
- 记录查看页

### 10.3 第三阶段：体验优化

在协议稳定后，再做重连提示、状态动画、错误提示和布局优化。

## 12. 前端分支隔离规则

- 前端任务只在前端分支处理，建议使用 `feat/frontend-xxx` 或 `fix/frontend-xxx`
- 不要在前端分支里修改后端 controller、service 或 repository
- 接口有变更时，先让后端分支完成冻结，再在前端分支适配
- 前端只负责界面、状态和客户端连接，不负责服务端状态同步实现

## 13. 前端接手优先文件

- [test-websocket.html](test-websocket.html) — 原始 WebSocket 测试页面（JSON 协议）
- [nginx-cluster.conf](nginx-cluster.conf) — Nginx 集群配置（WebSocket 代理、负载均衡策略）
- [demo/start-cluster.bat](demo/start-cluster.bat) — 集群一键启动脚本（含环境检查）
- [campus-battle-service/src/main/java/com/campusguess/battle/dto/BattleStateMessage.java](campus-battle-service/src/main/java/com/campusguess/battle/dto/BattleStateMessage.java) — 服务端推送消息结构
- [campus-battle-service/src/main/java/com/campusguess/battle/controller/BattleWebSocketHandler.java](campus-battle-service/src/main/java/com/campusguess/battle/controller/BattleWebSocketHandler.java) — WebSocket 处理器源码