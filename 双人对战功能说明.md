# 双人对战功能文档

## 功能概述

实现了基于WebSocket的双人实时对战功能，支持：
- 好友邀请机制
- 实时同步题目
- 双方作答后自动计算伤害
- 血量系统和胜负判定
- 多回合对战

## 技术架构

### 后端组件

1. **WebSocket配置** - [WebSocketConfig.java](src/main/java/com/campusguess/demo/config/WebSocketConfig.java)
   - STOMP协议支持
   - 端点：`/ws-battle`
   - 消息前缀：`/app`、`/topic`、`/queue`

2. **实体类** - [BattleRoom.java](src/main/java/com/campusguess/demo/model/entity/BattleRoom.java)
   - 存储对战房间状态
   - 记录玩家血量、答案、回合数等

3. **服务层** - [BattleService.java](src/main/java/com/campusguess/demo/service/BattleService.java)
   - 创建邀请、接受/拒绝邀请
   - 提交答案、计算伤害
   - 回合管理和胜负判定

4. **WebSocket控制器** - [BattleWebSocketController.java](src/main/java/com/campusguess/demo/controller/BattleWebSocketController.java)
   - 处理客户端消息
   - 推送实时状态更新

### DTO类

- `BattleInviteRequest` - 邀请请求
- `BattleInviteResponse` - 接受/拒绝响应
- `BattleAnswerRequest` - 作答请求
- `BattleStateMessage` - 状态消息（推送给客户端）

## API说明

### WebSocket端点

**连接地址**: `ws://localhost:8080/api/ws-battle`（支持SockJS降级）

### 消息订阅（客户端订阅）

| 订阅地址 | 说明 | 消息类型 |
|---------|------|---------|
| `/user/queue/battle/invite` | 接收对战邀请 | BattleStateMessage (type=INVITE) |
| `/user/queue/battle/state` | 接收对战状态更新 | BattleStateMessage |

### 消息发送（客户端发送）

| 发送地址 | 说明 | 请求体 |
|---------|------|--------|
| `/app/battle/invite` | 发起对战邀请 | BattleInviteRequest |
| `/app/battle/respond` | 响应邀请（接受/拒绝） | BattleInviteResponse |
| `/app/battle/answer` | 提交答案 | BattleAnswerRequest |

## 使用流程

### 1. 建立WebSocket连接

```javascript
const socket = new SockJS('http://localhost:8080/api/ws-battle');
const stompClient = Stomp.over(socket);

stompClient.connect({}, function(frame) {
    console.log('已连接');
    
    // 订阅邀请
    stompClient.subscribe('/user/queue/battle/invite', function(message) {
        const data = JSON.parse(message.body);
        console.log('收到邀请:', data);
    });
    
    // 订阅状态更新
    stompClient.subscribe('/user/queue/battle/state', function(message) {
        const data = JSON.parse(message.body);
        console.log('状态更新:', data);
    });
});
```

### 2. 发起对战邀请

```javascript
const request = {
    fromUsername: "user1",
    toUsername: "user2"
};

stompClient.send('/app/battle/invite', {}, JSON.stringify(request));
```

玩家B会收到类型为`INVITE`的消息：
```json
{
    "type": "INVITE",
    "roomCode": "ABC12345",
    "playerA": "user1",
    "playerB": "user2",
    "message": "user1 邀请你进行对战"
}
```

### 3. 接受/拒绝邀请

```javascript
// 接受邀请
const acceptResponse = {
    roomCode: "ABC12345",
    accepted: true,
    username: "user2"
};
stompClient.send('/app/battle/respond', {}, JSON.stringify(acceptResponse));

// 或拒绝邀请
const rejectResponse = {
    roomCode: "ABC12345",
    accepted: false,
    username: "user2"
};
stompClient.send('/app/battle/respond', {}, JSON.stringify(rejectResponse));
```

接受后，双方会收到`GAME_START`消息，包含第一道题目：
```json
{
    "type": "GAME_START",
    "roomCode": "ABC12345",
    "playerA": "user1",
    "playerB": "user2",
    "playerAHealth": 100,
    "playerBHealth": 100,
    "currentRound": 1,
    "question": {
        "id": 1,
        "imageKey": "xxxxx",
        "hint": "图书馆"
    },
    "message": "对战开始！"
}
```

### 4. 提交答案

```javascript
const answer = {
    roomCode: "ABC12345",
    username: "user1",
    longitude: 113.123456,
    latitude: 23.123456
};

stompClient.send('/app/battle/answer', {}, JSON.stringify(answer));
```

第一个玩家作答后，另一个玩家会收到`PLAYER_ANSWERED`消息：
```json
{
    "type": "PLAYER_ANSWERED",
    "roomCode": "ABC12345",
    "playerAAnswered": true,
    "playerBAnswered": false,
    "message": "user1 已作答",
    "countdown": 30
}
```

### 5. 回合结果

双方都作答后，会收到`ROUND_RESULT`消息：
```json
{
    "type": "ROUND_RESULT",
    "roomCode": "ABC12345",
    "playerAHealth": 100,
    "playerBHealth": 70,
    "currentRound": 1,
    "roundResult": {
        "playerADistance": 50.5,
        "playerBDistance": 350.8,
        "damagedPlayer": "user2",
        "damage": 30
    },
    "message": "第 1 回合结束"
}
```

### 6. 下一回合

回合结束3秒后，自动开始新回合，双方收到`NEW_QUESTION`消息：
```json
{
    "type": "NEW_QUESTION",
    "roomCode": "ABC12345",
    "currentRound": 2,
    "question": {
        "id": 2,
        "imageKey": "yyyyy",
        "hint": "食堂"
    },
    "playerAHealth": 100,
    "playerBHealth": 70,
    "playerAAnswered": false,
    "playerBAnswered": false,
    "message": "第 2 回合开始"
}
```

### 7. 游戏结束

当某方血量归零时，双方收到`GAME_OVER`消息：
```json
{
    "type": "GAME_OVER",
    "roomCode": "ABC12345",
    "playerA": "user1",
    "playerB": "user2",
    "playerAHealth": 60,
    "playerBHealth": 0,
    "winner": "user1",
    "message": "游戏结束！获胜者：user1"
}
```

## 伤害计算规则

基于答案与正确位置的距离（使用Haversine公式计算）：

- **0-100米**: 10点伤害
- **100-500米**: 20点伤害
- **500-1000米**: 30点伤害
- **>1000米**: 40点伤害

每位玩家初始血量为100点，血量归零即游戏结束。

## 测试步骤

### 方法1：使用提供的演示页面

1. 启动Spring Boot应用：
   ```bash
   cd demo
   ./mvnw spring-boot:run
   ```

2. 打开两个浏览器窗口/标签页，访问：
   ```
   http://localhost:8080/api/battle-demo.html
   ```

3. 在第一个窗口：
   - 输入用户名 `user1`，点击"连接"
   - 在邀请框输入 `user2`，点击"发起邀请"

4. 在第二个窗口：
   - 输入用户名 `user2`，点击"连接"
   - 等待收到邀请弹窗，点击"接受"

5. 双方交替作答，观察血量变化和回合进展

### 方法2：使用Postman测试（需配置STOMP）

可使用Postman的WebSocket功能，配置STOMP协议进行测试。

## 数据库表结构

```sql
CREATE TABLE battle_rooms (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    room_code VARCHAR(8) NOT NULL UNIQUE,
    player_a VARCHAR(50) NOT NULL,
    player_b VARCHAR(50) NOT NULL,
    player_a_health INT NOT NULL DEFAULT 100,
    player_b_health INT NOT NULL DEFAULT 100,
    current_question_id BIGINT,
    current_round INT NOT NULL DEFAULT 1,
    player_a_answered BOOLEAN NOT NULL DEFAULT FALSE,
    player_b_answered BOOLEAN NOT NULL DEFAULT FALSE,
    player_a_answer TEXT,
    player_b_answer TEXT,
    status VARCHAR(20) NOT NULL,
    winner VARCHAR(50),
    created_at DATETIME NOT NULL,
    started_at DATETIME,
    finished_at DATETIME
);
```

## 注意事项

1. **跨域配置**: 当前配置允许所有来源（`*`），生产环境需修改为具体域名
2. **用户认证**: 当前使用用户名标识，实际应用需集成JWT认证
3. **并发控制**: 同一用户同时只能参与一场对战
4. **题目随机**: 每回合从题库随机选择，需确保题库有足够题目
5. **倒计时**: 客户端需自行实现30秒倒计时逻辑
6. **断线重连**: 需要客户端实现断线重连和状态恢复

## 扩展建议

1. **观战模式**: 允许其他用户观看对战
2. **积分奖励**: 对战结束后给予积分奖励
3. **排行榜**: 统计对战胜率、连胜等
4. **道具系统**: 增加提示卡、时间延长等道具
5. **匹配系统**: 随机匹配陌生玩家对战
6. **重播功能**: 保存对战过程，支持回放

## 故障排查

### WebSocket连接失败
- 检查防火墙是否开放8080端口
- 确认CORS配置正确
- 查看浏览器控制台是否有跨域错误

### 消息接收不到
- 确认订阅地址正确（注意用户名前缀）
- 检查STOMP连接是否建立成功
- 查看后端日志确认消息是否发送

### 伤害计算错误
- 确认题目表有正确的经纬度数据
- 检查客户端提交的经纬度格式
- 查看后端日志中的距离计算结果

## 联系与支持

如有问题，请查看后端日志：
```bash
tail -f logs/spring.log
```

或在GitHub Issues中提问。
